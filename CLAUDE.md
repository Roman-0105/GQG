# GQS — контекст для Claude Code

Этот файл читается Claude Code автоматически при открытии проекта.
Здесь — сжатая история решений, чтобы не переспрашивать заново то, что
уже обсуждено и сделано.

## Что это за платформа

Веб-платформа (PWA) для учёта буровых работ и описания керна геологоразведочной
компании. Работает на ПК и смартфонах (Android/iPhone), с прицелом на офлайн-режим
в будущем (сотрудники в поле, слабая связь).

## Стек и архитектура

- **Frontend**: React + TypeScript + Vite, без своего backend-сервера
- **Backend**: Supabase (Postgres + Auth), доступ с фронтенда только через
  **publishable key** (безопасно светить в браузере) — **secret/service_role
  ключи в проект никогда не добавлялись и не должны добавляться**, вся
  защита данных — через RLS-политики в базе, а не через сокрытие кнопок в UI
- **Хостинг**: GitHub Pages (чисто статический — отсюда `HashRouter` вместо
  `BrowserRouter`, см. `src/main.tsx`), деплой через GitHub Actions при пуше в `main`
- **Миграции БД**: `supabase/migrations/*.sql`, применяются вручную через
  Supabase SQL Editor (копипаста), не через Supabase CLI — см. `supabase/README.md`

## Роли и права (см. ТЗ ниже, раздел 2)

Три роли: `general_director`, `technical_director` (права идентичны гендиру
везде), `party_chief` (начальник буровой партии/бригадир). Роль хранится в
таблице `profiles`, 1:1 с `auth.users`. Функция `is_management()` в БД
используется во всех RLS-политиках.

**Важно**: первого пользователя (гендира) создать через приложение нельзя —
курица-яйцо с RLS. Bootstrap только через SQL, см. `supabase/README.md`.
Дальше новых пользователей можно создавать через раздел "Пользователи" в UI.

## Ключевые решения, которые НЕ надо пересматривать без явного запроса

- **Бригадир = поле, выбираемое вручную** на каждом задании из списка
  пользователей с ролью `party_chief` (не привязан автоматически к участку) —
  сознательно так, после обсуждения с заказчиком
- **Задание "Описание керна"** может быть привязано к своей скважине
  (`drilling_task_id`) ИЛИ к скважине подрядчика (`external_*` поля, без
  персонала — у подрядчика свой персонал). Для скважины подрядчика обязателен
  `assigned_party_chief_id` — кто-то должен вносить сводки, а foreman там
  взять неоткуда
- **Суточная сводка НЕ хранится отдельно** — она считается на фронтенде как
  агрегация посменных сводок за дату. Только посменные сводки — реальные
  строки в таблице `reports`
- **Сводка при отправке всегда проходит через draft**: RLS на `report_costs`
  разрешает запись затрат только пока сводка `draft`, поэтому даже при "отправить
  на согласование" сначала сохраняем как draft + затраты, и только потом
  отдельным запросом меняем `approval_status` на `submitted` — см.
  `src/routes/reports/DailyReportForm.tsx`, там подробный комментарий про баг,
  который так чинили
- **При отклонении сводки** (`approval_status = 'rejected'`) автоматически
  ставится `edit_unlocked = true` — автор сразу может исправить и отправить
  заново, без отдельного запроса на правку через гендира
- **Экспорт — только .xlsx** (через SheetJS, `src/lib/xlsxExport.ts`), с
  **ленивой загрузкой** (`import()`) — библиотека тяжёлая (~300 КБ), не должна
  грузиться на каждой странице
- **Дизайн**: тёплая каменно-песочная палитра + мшисто-зелёный акцент
  (предметная область — керн/порода, не дефолтный SaaS-синий) — цвета
  зафиксированы, не менять без явного запроса. Токены — в `src/index.css`.
  Не возвращать дефолтные цвета Vite-шаблона
- **Редизайн 2026-09-17**: пара шрифтов вместо одного, все self-hosted через
  `@fontsource` (не Google Fonts CDN — важно для офлайн-режима): Fraunces на
  заголовках (характер "полевого дневника геолога"), Manrope в интерфейсе,
  JetBrains Mono на всех числах — глубины/проценты/даты/деньги (класс `.num`
  в `src/index.css`). Библиотеки — `framer-motion` (анимации, один "большой"
  момент — переход между страницами в `AppShell.tsx` — вместо разрозненных
  дёрганий) и `lucide-react` (иконки)
- **Оболочка приложения** — `src/components/layout/`: `TopNav` (верхнее меню,
  только ПК, >=768px) и `MobileHeader` + `BottomTabBar` (телефон, <768px) —
  переключаются медиа-запросом в CSS, не JS-детектом ширины. Пункты навигации
  и видимость по ролям — один источник `navItems.ts`, не дублировать в
  компонентах
- **План бурения — одно число (`planned_daily_meters`, м/сутки), не календарь
  по датам**. Накопительный план на дату считается на фронте как
  `min(planned_daily_meters × дней с начала, projected_depth)` — см.
  `src/routes/tasks/TaskDashboard.tsx`. Обсуждалось с заказчиком 17.09.2026:
  реальный план буровой задаётся как темп, а не разными числами на каждый
  день. Если практика покажет плавающий темп — тогда таблица план/дата, не раньше
- **План по керну/фотофиксации на дашборде задания — это `projected_depth`
  связанной скважины** (своей — через `drilling_task_id`, подрядчика — через
  `external_projected_depth`), отдельного поля плана у `core_description_tasks`
  нет: керн описывают вдоль того же ствола, второй план был бы дублем
- **`reports.shift_notes` — одно текстовое поле**, не отдельные "что сделано" /
  "что помешало" — бригадир пишет то и другое одним связным текстом (так
  описал заказчик пример: "5 из 10 по плану — потому что…")
- **Виды заданий (миграция 0007, 17.09.2026, из разбора реального отчёта
  заказчику) — 4 штуки, у каждого своя логика назначения ответственного**:
  `drilling_tasks`, `core_description_tasks`, `core_sawing_tasks`
  (распиловка), `sampling_tasks` (опробование). Единый список и подписи —
  `src/types/taskType.ts`, не заводить локальные `type TaskType = ...` по файлам.
  - **Геологическая vs геотехническая документация** — это ДВА отдельных
    `core_description_tasks` на одну скважину (`documentation_type`), не два
    числовых поля на одном — идут независимым темпом, может быть разный
    документатор
  - **Распиловка — только своя скважина**, ответственный НЕ назначается
    отдельно: всегда `foreman_id` связанного `drilling_task_id` (решение
    заказчика — тот же бригадир, что бурит). Смены называются "День"/"Ночь"
    в интерфейсе — это те же `shift_number` 1/2, просто другая подпись
  - **Опробование — ответственного назначают ЯВНО ВСЕГДА**, даже на своей
    скважине (в отличие от описания керна, где это обязательно только для
    скважины подрядчика) — это может быть отдельная бригада, не буровая.
    Метрики — `samples_taken`/`samples_submitted`, без смен вообще
- **Копирование сводки в WhatsApp** (`src/lib/whatsappMessage.ts`) — формат
  подогнан 1:1 под реальный внутренний отчёт заказчика (17.09.2026), не
  придуман: "Отчёт по бурению {ДД.ММ.ГГГГ}" / "Смена {N}" / пустая строка /
  "Скважина: …" / "Буровая установка: …" / "Глубина: {забой}" / "Проходка:
  {метраж смены}" / пустая строка / текст из "Что сделано за смену". Часы
  работы в это сообщение НЕ входят. Отчёт для заказчика (сводный по всем
  скважинам участка сразу) — отдельная, более крупная задача, сознательно
  отложена вторым этапом
- **`npx tsc --noEmit` не ловит всё** — этот проект собирается через `tsc -b`
  (project references, см. `npm run build`), и он строже: минимум один раз
  словил ошибку индексации `Record<TaskType, …>` значением `TaskType |
  undefined` из `useParams()`, которую `tsc --noEmit` пропустил. Перед
  коммитом — всегда `npm run build`, не только `--noEmit`
- **Работы, прицепленные к заданию на бурение (19.09.2026)** — по решению
  заказчика: если керн/распиловка/опробование ведутся на ТОЙ ЖЕ скважине,
  что и бурение (частый случай в контрактах), их больше не обязательно
  заводить отдельным заданием через "Участок" — можно прицепить прямо в
  форме правки задания на бурение (`DrillingTaskForm.tsx`, правая колонка,
  только в режиме правки — для нового задания сначала нужно его сохранить).
  Технически это ВСЁ ТЕ ЖЕ строки в `core_description_tasks`/
  `core_sawing_tasks`/`sampling_tasks` с `drilling_task_id`, что и раньше —
  никакой миграции не потребовалось, RLS не менялась. Новое — только UI:
  - `src/components/Attached{CoreDescription,Sawing,Sampling}Card.tsx` —
    компактные карточки "добавить/редактировать" внутри DrillingTaskForm.
    На скважине может быть ОБЕ документации керна одновременно (геологическая
    и геотехническая — два разных `core_description_tasks`, см. миграцию 0007).
  - **Сводка по бурению** (`DailyReportForm.tsx`, только `taskType==='drilling'`)
    подтягивает прицепленные задачи и показывает для них доп. поля прямо в
    той же форме (интервалы керна на каждую документацию, метраж распиловки,
    пробы). При сохранении это по-прежнему ОТДЕЛЬНЫЕ строки `reports` (по
    одной на прицепленную задачу, `saveAttachedReport`) с той же датой/сменой/
    часами/комментарием, что и у сводки по бурению — **согласование
    остаётся раздельным по каждому виду работ** (решение от 19.09.2026,
    техдир может одобрить бурение и отдельно отклонить, например,
    опробование той же смены). Костыль-но-нужный момент: при правке уже
    существующей сводки по бурению форма ищет "сиблингов" — строки
    прицепленных работ с той же датой/сменой — и обновляет их, а не
    дублирует; это происходит один раз при загрузке формы, не реактивно
    при смене даты в уже открытой форме (если дата меняется вручную на
    уже занятую — исходный риск дублирования тот же, что был всегда для
    опробования, см. ниже). Если сиблинг уже НЕ в статусе draft/edit_unlocked
    (техдир успел одобрить/просмотреть его отдельно от бурения) — форма
    показывает его поля только для чтения (`siblingLocked`) и не трогает эту
    строку при сохранении вообще: RLS всё равно не даст её обновить (0 строк,
    та же ошибка PGRST116, что чинили 19.09.2026 в другом месте), а вставка
    новой строки рядом вместо обновления задвоила бы метраж/пробы после
    одобрения обеих. Проверено вживую 19.09.2026 под реальным бригадиром:
    отправка объединённой сводки создаёт ровно 5 раздельных строк с общими
    датой/сменой/часами, повторное открытие на правку находит все сиблинги
    и обновляет их без дублей (одна новая строка — только для впервые
    заполненного вида работы).
  - Опробование можно вписать в объединённую форму, ТОЛЬКО если
    `assigned_party_chief_id` прицепленного задания = текущий пользователь
    (иначе `reports_insert_own` не даст записать строку от его имени) —
    по умолчанию при прицеплении подставляется бригадир бурения, но поле
    редактируемое (может быть другая бригада/лаборатория). Если
    ответственный — не бригадир бурения, тот вносит сводки по опробованию
    по-старому, через самостоятельное задание
  - Копирование в WhatsApp (`buildDrillingShiftMessage`) — доп. строки
    (керн/распиловка/пробы) добавляются ТОЛЬКО если задача прицеплена И
    что-то реально введено; без вложений формат не меняется ни на символ
    (он подогнан 1:1 под реальный отчёт заказчика, см. запись выше)
  - Затраты (`report_costs`) остаются привязаны только к строке бурения —
    не дублируются и не делятся между прицепленными работами той же смены
  - `SiteDetail.tsx` — керн/распиловка/опробование со `drilling_task_id`
    теперь рендерятся ВЛОЖЕННЫМИ в карточку своей скважины (`.attached-row-*`
    в index.css), а не отдельными списками — отдельные списки остались
    только для скважины подрядчика (`external_well_number`, нет
    `drilling_task_id`). Распиловка не может быть на скважине подрядчика
    вообще (см. запись про миграцию 0007) — для неё отдельного списка
    больше нет совсем
  - Старые самостоятельные формы создания (кнопки "+ Задание: ..." в шапке
    участка) НЕ убраны — работают как раньше, это по-прежнему единственный
    способ завести задание на скважину подрядчика. Для своей скважины
    предпочтительный путь теперь — через правку задания на бурение
  - Не реализовано сознательно (см. диалог 19.09.2026, "выполним позже"):
    аналогичное прицепление подзадач к ЗАДАНИЯМ-ЯКОРЯМ другого типа
    (тендер только на описание керна/только на опробование скважины
    подрядчика) — сейчас якорем может быть только бурение
- **Левый сворачиваемый тулбар вместо верхнего меню (19.09.2026)** —
  `TopNav.tsx` заменён на `src/components/layout/Sidebar.tsx` (только ПК,
  >=768px, см. `.sidebar` в `index.css`). Свёрнутое/развёрнутое состояние —
  `localStorage` (`gqs-sidebar-collapsed`), не БД: это предпочтение
  конкретного браузера, не общий параметр проекта. `NAV_ITEMS` (единый
  источник для сайдбара/мобильного таб-бара) не менялся по структуре — просто
  рендерится вертикально. Общие классы `.brand`/`.brand-mark` (лого) теперь
  используются и сайдбаром, и `MobileHeader.tsx` — не дублировать под новым
  именем. `UserMenu.tsx` принимает `placement="up"` (сайдбар открывает меню
  вверх, снизу экрана — вниз было бы за пределами вьюпорта) и
  `showLabel` (в развёрнутом сайдбаре показывает имя/роль рядом с аватаром)
- **Кнопка "Сводка" на карточке участка (19.09.2026)** — раньше вела сразу на
  создание новой сводки (`/reports/new`), теперь открывает список
  ("историю") сводок задания (`/reports`) — создание сводки осталось
  доступно из самого списка (кнопка "Новая сводка") и через отдельную
  одноимённую кнопку внутри `TaskDashboard.tsx`, которая по-прежнему ведёт
  на `/reports/new`. Два визуально похожих места, разное поведение — так и
  задумано (список — обзор, дашборд задания — быстрое добавление)
- **"Мои сводки" — новый экран для бригадира (19.09.2026)**,
  `src/routes/reports/MyReports.tsx`, роут `/reports/mine`, пункт меню
  только для `party_chief`. Решает то, что список сводок ОДНОГО задания
  (`TaskReportsList.tsx`) в принципе не может показать: сквозную историю
  сразу по всем заданиям бригадира (все скважины, все виды работ).
  Категории — по факту (задание, у которого есть хотя бы одна своя сводка),
  не по заданиям без сводок. Категория при клике открывает модалку
  (`.modal-backdrop`/`.modal-panel` в index.css — переиспользовать для
  будущих модалок) с постраничным списком (`PAGE_SIZE = 3`) и ссылкой
  "Открыть полный список" на тот же `TaskReportsList.tsx`. Данные о своих
  заданиях (`drilling_tasks`/`core_description_tasks`/...) не фильтруются
  на фронте по бригадиру — RLS уже отдаёт только "свои" (`foreman_id`/
  `assigned_party_chief_id = auth.uid()`)
- **Двойной сабмит сводки (19.09.2026)** — `DailyReportForm.tsx` `handleSubmit`
  защищён синхронным `useRef`-флагом (`submittingRef`), а не только
  `disabled={submitting !== null}` на кнопке. Реальный баг: быстрый повторный
  тап (характерно на телефоне при пересдаче отклонённой сводки) успевает
  вызвать `handleSubmit` дважды ДО того, как React перерисует `disabled` —
  React-стейт обновляется асинхронно. Первый вызов доводит сводку до
  `submitted` (снимает `edit_unlocked`), второй параллельный вызов на этапе
  черновика делает `update().select().single()` по уже неэдитируемой (по RLS)
  строке → 0 строк → PostgREST `PGRST116` `"Cannot coerce the result to a
  single JSON object"`. Если увидите этот текст ошибки где-то ещё в проекте —
  ищите такой же паттерн (async-обработчик без синхронной защиты от повторного
  вызова), не RLS-политику
- **Правка заданий (17.09.2026)** — все 4 формы заданий (`*TaskForm.tsx`)
  теперь двойного назначения: `taskId` в URL (`/sites/:siteId/tasks/:type/:taskId/edit`)
  включает режим правки той же формой, что и создание — insert/update
  собираются как ДВА раздельных вызова с разными объектами полей (`status`
  только при update, `created_by` только при insert), не один объект с
  условным спредом в один `insert/update` через тернарник — так ловилась
  ошибка типов на границе union (`tsc -b` ловит, `--noEmit` иногда нет, см.
  выше). Диаметры при правке — снести старые строки и записать текущее
  состояние формы заново (тот же приём, что с затратами сводки)
- **Двухуровневые статьи затрат (19.09.2026)** — раньше `cost_categories`
  была плоским списком ("ГСМ", "Коронки", "Материалы (прочее)"), единица
  измерения была на самой категории, а сам справочник правился только через
  SQL/Table Editor. По запросу заказчика (тестировал сводку на телефоне,
  захотел сам добавлять статьи) сделано два уровня: категория (просто
  название) → виды затрат внутри неё (название + единица измерения),
  миграция `0008_cost_items.sql`, новая таблица `cost_items` с
  `category_id`. `report_costs` теперь ссылается на `cost_item_id`
  (было `cost_category_id`) — колонка `unit` с `cost_categories` перенесена
  на `cost_items`. Старые 3 категории переносятся 1:1 в виды затрат тем же
  названием/единицей, чтобы существующие `report_costs` не потерялись.
  - `src/routes/settings/CostCategoriesSettings.tsx` — экран управления,
    доступен только management (как "Пользователи"), пункт меню "Статьи
    затрат". "+ Добавить категорию" наверху, у каждой категории — карточка
    со списком видов и своей мини-формой "+ Добавить вид затрат" (имя +
    единица измерения: выбор из частых вариантов шт/л/м/м³/т/кг/компл. или
    "другая…" с ручным вводом) — по образцу `Attached*Card.tsx` (сама
    пишет в базу, сообщает наверх колбэком, без перезагрузки списка)
  - `CostRowsEditor.tsx` (используется в `DailyReportForm.tsx`) — выбор
    затрат в сводке стал каскадным: сначала категория, затем вид затрат
    внутри неё (список видов фильтруется на клиенте по `category_id`)
  - `ReportDetail.tsx`/`ReportReview.tsx`/`SummaryReport.tsx` — везде, где
    раньше показывалось название категории у строки затрат, теперь
    показывается `"Категория — Вид"` (join через `cost_items.category_id`)
  - **Важно**: миграция `0008` ДРОПАЕТ `cost_categories.unit` и
    `report_costs.cost_category_id` — до применения владельцем через SQL
    Editor приложение продолжает работать на старой схеме без ошибок
    (запрос к ещё не существующей `cost_items` просто возвращает пустой
    список, дропдаун "вид затрат" остаётся пустым), но добавить затраты в
    сводке нельзя, пока миграция не накатана

- **Организации бурения, буровые станки, работники (19.09.2026)** — по
  запросу заказчика во время создания задания на бурение: раньше номер
  станка был свободным текстом, организации/работники правились только
  через SQL. Миграция `0009_drilling_rigs_and_workers.sql`.
  - **Буровые станки** — новая таблица `drilling_rigs` (номер, марка/модель,
    вид бурения — колонковое/шнековое/роторное/ударно-канатное или свой
    вариант). Станок **навсегда закреплён за одной организацией**
    (`organization_id`, своей или подрядчика) — решение заказчика, передача
    между организациями не предусмотрена. `drilling_tasks.rig_number`
    (свободный текст) заменён на `drilling_rig_id` (FK) — старые значения
    переносятся 1:1 в справочник при миграции, привязка к заданию
    сохраняется. `src/routes/tasks/DrillingTaskForm.tsx` — выбор каскадный:
    сначала организация, потом станок внутри нее (список фильтруется на
    клиенте по `organization_id`, сбрасывается при смене организации)
  - **Организации бурения** — экран
    `src/routes/settings/DrillingOrganizationsSettings.tsx` (management
    only, пункт меню "Организации бурения"): "+ Добавить организацию"
    наверху (новые всегда `is_own=false`, "своей" может быть только та,
    что отмечена в БД — обычно ровно одна), у каждой организации —
    раскрывающаяся карточка со станками внутри и формой "+ Добавить
    станок" — тот же UI-паттерн, что и в `CostCategoriesSettings.tsx`
    (категория → виды затрат)
  - **Работники** — новая сущность `workers`, **сознательно НЕ связана с
    `profiles`/`auth.users`** (решение заказчика: это справочник состава
    бригады для учёта, а не логин-аккаунты, в отличие от бригадира
    `party_chief`). Поля: ФИО, должность (свободный текст), организация,
    `assigned_foreman_id` (ссылка на `profiles` с ролью `party_chief`) —
    **текущая** привязка к бригадиру, истории смен не ведём (сознательно
    просто, при необходимости бригадир меняется вручную в любой момент).
    Экран `src/routes/settings/WorkersSettings.tsx` (management only,
    пункт меню "Работники"): форма добавления + список карточек с
    фильтрами по организации/бригадиру и инлайн-select смены бригадира
    прямо на карточке (сохраняется сразу при выборе, без отдельной кнопки
    "сохранить") — это и есть "распределение работников по бригадирам"
  - RLS у всех трёх новых/изменённых таблиц — как у остальных справочников
    (`cost_categories` и т.п.): SELECT всем авторизованным, INSERT/UPDATE/
    DELETE только `is_management()`
  - Баг, пойманный вживую при тестировании под гендиром: раскрывающая
    кнопка категории/организации (`background: transparent`) наследовала
    глобальный `color: #fff` от базового стиля `button` в `index.css` —
    текст названия был белым по белому/кремовому фону, невидимым, хотя в
    DOM он был. Чинится явным `color: var(--color-text)` на самой кнопке
    — если где-то ещё в проекте появится прозрачная кнопка поверх
    `<button>`, нужно так же не забывать явно задавать цвет текста, не
    полагаться на дефолт

- **Формы добавления — только в модалке (19.09.2026)** — по отзыву
  заказчика: раньше формы "добавить X" либо всегда были развёрнуты наверху
  списка (Участки, Пользователи, Работники, категории/организации), либо
  появлялись инлайн внутри карточки при клике (виды затрат внутри
  категории, станки внутри организации) — хотелось единообразия и чтобы
  форма не терялась среди остального контента. Теперь везде: кнопка
  "+ Добавить …" (у страниц — справа от заголовка `<h1>`, у карточек —
  внутри развёрнутого блока) открывает `src/components/Modal.tsx` —
  переиспользуемый компонент (заблюренный/затемнённый фон на весь экран,
  закрытие по клику на фон или крестик), тот же визуальный паттерн, что
  уже был в `MyReports.tsx` для просмотра истории сводок (`.modal-backdrop`/
  `.modal-panel` в `index.css`, теперь `.modal-backdrop` ещё и с
  `backdrop-filter: blur(4px)`). Затронуты: `SitesList.tsx`,
  `UsersList.tsx`, `CostCategoriesSettings.tsx` (и категория, и вложенный
  вид затрат), `DrillingOrganizationsSettings.tsx` (и организация, и
  вложенный станок), `WorkersSettings.tsx`. Форма при успехе обычно
  закрывает модалку и сбрасывает поля; ошибка/успех показываются ВНУТРИ
  модалки (не на странице позади неё, иначе не видно). У `UsersList.tsx`
  модалка НЕ закрывается автоматически при успехе — сообщение
  "пользователь создан, сообщите пароль лично" должно остаться на экране,
  пока не закроют вручную (было так и раньше, просто в развёрнутой форме)

- **Распределение работников по задачам (20.09.2026, по запросу
  заказчика)** — миграция `0010_task_worker_assignments.sql`, новая
  таблица `task_worker_assignments`. Раньше "работники" (миграция 0009)
  были привязаны только к бригадиру ("своя бригада"), но не к конкретному
  заданию/скважине. Теперь бригадир (или тот, кто назначен ответственным
  за конкретное задание — `assigned_party_chief_id` на скважине
  подрядчика) может расписать, КТО из его бригады выполняет какую роль на
  ЭТОЙ конкретной скважине/задании: буровик и помощник(и) бурильщика — на
  бурении (может быть несколько помбуров сразу, буровик тоже технически не
  ограничен числом, хотя обычно один), ответственный исполнитель — на
  описании керна/распиловке/опробовании. **Это чисто учётная информация**
  (кто по факту делал) — НЕ меняет, кто согласовывает сводки и кто может
  их подавать (тот механизм — `foreman_id`/`assigned_party_chief_id` на
  самих таблицах заданий — не тронут). В частности, по распиловке
  (см. запись про решение 19.09.2026 — согласовывает всегда бригадир
  бурения) теперь ДОПОЛНИТЕЛЬНО можно отметить конкретного
  работника-исполнителя, это не отменяет то правило.
  - Работника можно выбрать ТОЛЬКО из бригады того, кто отвечает за задание
    (`workers.assigned_foreman_id` = бригадир скважины/ответственный) — и
    это проверяется не только в UI, но и в RLS (`with check` в
    `task_worker_assignments_own_foreman`), т.к. вся защита данных в
    проекте — через RLS, не через сокрытие кнопок
  - **Право редактировать распределение — не только у management.**
    Бригадир (или ответственный за скважину подрядчика) тоже может сам
    распределять СВОИХ работников по СВОИМ заданиям — RLS-политика
    `task_worker_assignments_own_foreman` смоделирована 1:1 по структуре
    `reports_insert_own` (EXISTS по каждому из 4 видов заданий, тот же
    принцип владения). Это № первый случай в проекте, где party_chief
    получает право писать НЕ в `reports`/`report_costs`, а в
    "родительскую" сущность задания — но только в отдельную таблицу
    `task_worker_assignments`, не в сами `drilling_tasks` и т.п. (та форма
    редактирования задания остаётся management-only, см. запись про
    `DrillingTaskForm.tsx` выше)
  - UI — новый компонент `src/components/CrewAssignmentSection.tsx`,
    встроен в `TaskDashboard.tsx` (общий дашборд для всех 4 видов заданий,
    туда и так уже заходит и party_chief на свои задания, и management —
    так избежали дублирования прав доступа в отдельной форме). Показывает
    роли для этого вида задания (`ROLE_SLOTS`), список уже назначенных
    работников чипами с крестиком (удалить) и, если есть право
    редактировать, select "+ добавить работника" из бригады. Право
    редактировать = `isManagement() || foreman?.id === profile?.id`, где
    `foreman` — это уже загруженный в `TaskDashboard` "ответственный за
    задание" профиль (там это поле исторически называется `foreman` для
    всех 4 видов, включая `assigned_party_chief_id` внешних заданий)
  - Уникальность (задание, роль, работник) — через 4 частичных уникальных
    индекса (по одному на вид задания), а не один `UNIQUE` по всем 4
    FK-колонкам сразу: обычный составной `UNIQUE` не сработал бы, так как
    в Postgres `NULL` никогда не равен `NULL`, а у каждой строки 3 из 4
    FK-колонок всегда `NULL` (см. `one_task_source`) — единый `UNIQUE`
    просто никогда бы не сработал. Та же ловушка, что и с индексами на
    `reports`, если там когда-нибудь понадобится аналогичная уникальность
  - **Модалка "Состав бригады" вместо инлайн-блока (20.09.2026)** — по
    отзыву заказчика: кнопка "Состав бригады" теперь в шапке дашборда
    задания (`TaskDashboard.tsx`), справа от названия скважины (`h1` с
    `flex: 1` подвигает кнопку в крайнее правое положение). Открывает
    `Modal` с одной или несколькими секциями `CrewAssignmentSection`
    сразу — если это дашборд БУРЕНИЯ, внутри ОДНОЙ модалки показываются
    ВСЕ применимые секции: само бурение (буровик/помбур) + КАЖДАЯ
    прицепленная к этой скважине работа (геологическая/геотехническая
    документация керна, распиловка, опробование — грузятся тем же
    запросом по `drilling_task_id`, что в `DrillingTaskForm.tsx`), у
    каждой своя подпись (`heading` проп) и свой `canEdit`/`foremanId`
    (для опробования и внешне назначенного описания керна ответственный
    может отличаться от бригадира бурения — `assigned_party_chief_id`
    задания, если задан, иначе бригадир). Это решает жалобу "не могу
    найти, где назначать исполнителя по доп. заданиям" — раньше пришлось
    бы отдельно открывать дашборд каждого прицепленного задания. Дашборды
    остальных 3 видов заданий (когда открыты НЕ как прицепленные, а
    напрямую — например, скважина подрядчика) по-прежнему показывают
    свою кнопку/модалку с одной секцией на себя же — компонент
    `CrewAssignmentSection` от этого не изменился, только вызывающий код
    в `TaskDashboard.tsx` решает, сколько секций показать разом

- **Название участка/скважины в сводке (20.09.2026, по отзыву заказчика)**
  — на согласовании (`ReportReview.tsx`) и в просмотре сводки автором
  (`ReportDetail.tsx`) не было видно, к какой скважине/участку относится
  сводка (было только в списке "На согласовании" ДО клика, а после —
  терялось). Добавлена общая функция `src/lib/reportLabel.ts` →
  `loadReportSiteAndWellLabel(report)` — по одной сводке достаёт название
  участка + подпись задания (тип работы + своя/подрядчика скважина + №),
  используется в обоих местах. Заодно найден и починен смежный баг в
  `PendingApprovals.tsx` (список "На согласовании"): номер скважины у
  сводок по ПРИЦЕПЛЕННЫМ подзаданиям (керн/распиловка/опробование) не
  подтягивался вообще (показывал "?"), потому что `drilling_task_id` у
  таких сводок лежит не в `reports.drilling_task_id` (там `null`), а на
  уровень глубже — в самой подзадаче (`core_description_tasks.
  drilling_task_id` и т.п.). Список `drilling_tasks` для подгрузки номеров
  теперь запрашивается ВТОРЫМ шагом, после того как загружены сами
  подзадачи и стали известны их вложенные `drilling_task_id`
- **Удаление сводок (20.09.2026, по запросу заказчика)** — миграция
  `0011_reports_delete.sql`. На `reports` вообще не было DELETE-политики
  (см. CLAUDE.md, было явно зафиксировано ограничение) — теперь автор
  может удалить СВОЙ черновик (`approval_status = 'draft'`, отправленную/
  согласованную/отклонённую так удалить нельзя — это уже история),
  management — сводку в любом статусе (уборка тестовых/ошибочных данных).
  Кнопка "Удалить черновик"/"Удалить сводку" — в `ReportDetail.tsx`, рядом
  с кнопкой редактирования, с двухшаговым подтверждением прямо на странице
  (не `window.confirm()` — в проекте так нигде не делали, держим
  единообразный стиль). **Важная ловушка, поймана вживую при тестировании**:
  `supabase.from('reports').delete().eq('id', id)` без RLS-политики,
  разрешающей удаление, возвращает `error: null` и просто не трогает ни
  одной строки — БЕЗ `.select()` после `.delete()` это выглядит как
  успех (код думает, что удалил, и уходит со страницы), хотя на деле
  строка осталась в базе. Обязательно проверять `data`/`deletedRows` на
  пустоту, а не только `error`, после `delete()` — актуально для ЛЮБОГО
  будущего `delete()` в проекте, не только этого

- **Категоризация "Сводок на согласовании" (20.09.2026, по запросу
  заказчика)** — `PendingApprovals.tsx` был плоским списком, отсортированным
  только по времени отправки; при нескольких участках сразу было сложно
  ориентироваться. Теперь трёхуровневая группировка: **участок → скважина/
  задание → вид работ под заданием** (`groupReports()`), плюс 3 фильтра
  сверху — по участку, по заданию (сужается каскадно под выбранный
  участок) и по бригадиру (по автору сводки). Участки — раскрывающиеся
  карточки (chevron, тот же паттерн, что в `CostCategoriesSettings.tsx` и
  т.п.), по умолчанию все развёрнуты; скважина и вид работ внутри —
  просто визуальная группировка, не сворачиваются (обычно там всего
  несколько строк, доп. клики не нужны). Ключ группировки "скважина" —
  `drilling_task_id`, ОБЩИЙ для бурения и всех прицепленных к нему работ
  (см. отзыв 19.09.2026 про прицепленные подзадачи) — поэтому сводка по
  бурению и сводка по геологической документации той же скважины
  попадают в одну группу; для скважины подрядчика (нет `drilling_task_id`)
  используется синтетический ключ на саму подзадачу
- **Синхронизирована с "Мои сводки"** идея группировки, но здесь
  дополнительно есть фильтры (в `MyReports.tsx` их нет — там и так уже
  разбито по заданиям бригадира персонально)

## Не реализовано (сознательно отложено, не путать с "забыли")

- **Офлайн-режим** (IndexedDB, очередь синхронизации) — самая трудоёмкая
  часть, отдельный будущий этап
- **Табель** — структура не определена, заказчик отложил вопрос
- Управление справочниками (организации-подрядчики, категории затрат) —
  сейчас правятся только через SQL/Table Editor в Supabase напрямую, экрана
  в UI нет
- **Автогенерация сводного отчёта заказчику** (по всем скважинам/заданиям
  участка сразу, формат из переписки 17.09.2026) — сознательно вторым
  этапом, после того как схема заданий (распиловка/опробование/документация)
  обкатается на реальных данных

## Workflow

- Ветка на фичу → PR → **обязательно `npm run build` и `npx tsc --noEmit`
  перед коммитом** → описание PR по-русски, подробное, с объяснением "почему",
  не только "что"
- Если PR меняет `supabase/migrations/` — миграцию нужно ОТДЕЛЬНО применить
  в Supabase SQL Editor (владелец проекта делает это вручную, скопировав SQL)
  — само по себе она не применяется автоматически нигде
- Полное ТЗ (все версии решений, включая отклонённые варианты и почему) —
  в истории переписки с заказчиком в claude.ai, не продублировано в репозитории
  файлом. Если нужны детали, которых нет в этом файле — переспросить заказчика,
  не додумывать самостоятельно

# GQG — контекст для Claude Code

Этот файл читается Claude Code автоматически при открытии проекта.
Здесь — сжатая история решений, чтобы не переспрашивать заново то, что
уже обсуждено и сделано.

## Что это за платформа

Веб-платформа (PWA) для учёта буровых работ и описания керна геологоразведочной
компании. Работает на ПК и смартфонах (Android/iPhone), с прицелом на офлайн-режим
в будущем (сотрудники в поле, слабая связь).

## Стек и архитектура

- **Frontend**: React + TypeScript + Vite, без своего backend-сервера
- **Backend**: Supabase (Postgres + Auth), доступ с фронтенда только через
  **publishable key** (безопасно светить в браузере) — **secret/service_role
  ключи в проект никогда не добавлялись и не должны добавляться**, вся
  защита данных — через RLS-политики в базе, а не через сокрытие кнопок в UI
- **Хостинг**: GitHub Pages (чисто статический — отсюда `HashRouter` вместо
  `BrowserRouter`, см. `src/main.tsx`), деплой через GitHub Actions при пуше в `main`
- **Миграции БД**: `supabase/migrations/*.sql`, применяются вручную через
  Supabase SQL Editor (копипаста), не через Supabase CLI — см. `supabase/README.md`

## Роли и права (см. ТЗ ниже, раздел 2)

Три роли: `general_director`, `technical_director` (права идентичны гендиру
везде), `party_chief` (начальник буровой партии/бригадир). Роль хранится в
таблице `profiles`, 1:1 с `auth.users`. Функция `is_management()` в БД
используется во всех RLS-политиках.

**Важно**: первого пользователя (гендира) создать через приложение нельзя —
курица-яйцо с RLS. Bootstrap только через SQL, см. `supabase/README.md`.
Дальше новых пользователей можно создавать через раздел "Пользователи" в UI.

## Ключевые решения, которые НЕ надо пересматривать без явного запроса

- **Бригадир = поле, выбираемое вручную** на каждом задании из списка
  пользователей с ролью `party_chief` (не привязан автоматически к участку) —
  сознательно так, после обсуждения с заказчиком
- **Задание "Описание керна"** может быть привязано к своей скважине
  (`drilling_task_id`) ИЛИ к скважине подрядчика (`external_*` поля, без
  персонала — у подрядчика свой персонал). Для скважины подрядчика обязателен
  `assigned_party_chief_id` — кто-то должен вносить сводки, а foreman там
  взять неоткуда
- **Суточная сводка НЕ хранится отдельно** — она считается на фронтенде как
  агрегация посменных сводок за дату. Только посменные сводки — реальные
  строки в таблице `reports`
- **Сводка при отправке всегда проходит через draft**: RLS на `report_costs`
  разрешает запись затрат только пока сводка `draft`, поэтому даже при "отправить
  на согласование" сначала сохраняем как draft + затраты, и только потом
  отдельным запросом меняем `approval_status` на `submitted` — см.
  `src/routes/reports/DailyReportForm.tsx`, там подробный комментарий про баг,
  который так чинили
- **При отклонении сводки** (`approval_status = 'rejected'`) автоматически
  ставится `edit_unlocked = true` — автор сразу может исправить и отправить
  заново, без отдельного запроса на правку через гендира
- **Экспорт — только .xlsx** (через SheetJS, `src/lib/xlsxExport.ts`), с
  **ленивой загрузкой** (`import()`) — библиотека тяжёлая (~300 КБ), не должна
  грузиться на каждой странице
- **Дизайн**: тёплая каменно-песочная палитра + мшисто-зелёный акцент
  (предметная область — керн/порода, не дефолтный SaaS-синий). Токены — в
  `src/index.css`. Не возвращать дефолтные цвета Vite-шаблона.
  **Обновление 25.09.2026**: формулировка "цвета зафиксированы, не менять
  без явного запроса" была явно снята владельцем платформы при заказе
  полного редизайна — см. запись про "Направление А" ниже. Общая
  философия (тёплая, не-SaaS, керн/порода) сохранена, конкретные hex —
  нет, актуальные всегда смотреть в `src/index.css`, не в старых записях
  этого файла
- **Редизайн 2026-09-17**: пара шрифтов вместо одного, все self-hosted через
  `@fontsource` (не Google Fonts CDN — важно для офлайн-режима): Fraunces на
  заголовках (характер "полевого дневника геолога"), Manrope в интерфейсе,
  JetBrains Mono на всех числах — глубины/проценты/даты/деньги (класс `.num`
  в `src/index.css`). Библиотеки — `framer-motion` (анимации, один "большой"
  момент — переход между страницами в `AppShell.tsx` — вместо разрозненных
  дёрганий) и `lucide-react` (иконки)
- **Оболочка приложения** — `src/components/layout/`: `TopNav` (верхнее меню,
  только ПК, >=768px) и `MobileHeader` + `BottomTabBar` (телефон, <768px) —
  переключаются медиа-запросом в CSS, не JS-детектом ширины. Пункты навигации
  и видимость по ролям — один источник `navItems.ts`, не дублировать в
  компонентах
- **План бурения — одно число (`planned_daily_meters`, м/сутки), не календарь
  по датам**. Накопительный план на дату считается на фронте как
  `min(planned_daily_meters × дней с начала, projected_depth)` — см.
  `src/routes/tasks/TaskDashboard.tsx`. Обсуждалось с заказчиком 17.09.2026:
  реальный план буровой задаётся как темп, а не разными числами на каждый
  день. Если практика покажет плавающий темп — тогда таблица план/дата, не раньше
- **План по керну/фотофиксации на дашборде задания — это `projected_depth`
  связанной скважины** (своей — через `drilling_task_id`, подрядчика — через
  `external_projected_depth`), отдельного поля плана у `core_description_tasks`
  нет: керн описывают вдоль того же ствола, второй план был бы дублем
- **`reports.shift_notes` — одно текстовое поле**, не отдельные "что сделано" /
  "что помешало" — бригадир пишет то и другое одним связным текстом (так
  описал заказчик пример: "5 из 10 по плану — потому что…")
- **Виды заданий (миграция 0007, 17.09.2026, из разбора реального отчёта
  заказчику) — 4 штуки, у каждого своя логика назначения ответственного**:
  `drilling_tasks`, `core_description_tasks`, `core_sawing_tasks`
  (распиловка), `sampling_tasks` (опробование). Единый список и подписи —
  `src/types/taskType.ts`, не заводить локальные `type TaskType = ...` по файлам.
  - **Геологическая vs геотехническая документация** — это ДВА отдельных
    `core_description_tasks` на одну скважину (`documentation_type`), не два
    числовых поля на одном — идут независимым темпом, может быть разный
    документатор
  - **Распиловка — только своя скважина**, ответственный НЕ назначается
    отдельно: всегда `foreman_id` связанного `drilling_task_id` (решение
    заказчика — тот же бригадир, что бурит). Смены называются "День"/"Ночь"
    в интерфейсе — это те же `shift_number` 1/2, просто другая подпись
  - **Опробование — ответственного назначают ЯВНО ВСЕГДА**, даже на своей
    скважине (в отличие от описания керна, где это обязательно только для
    скважины подрядчика) — это может быть отдельная бригада, не буровая.
    Метрики — `samples_taken`/`samples_submitted`, без смен вообще
- **Копирование сводки в WhatsApp** (`src/lib/whatsappMessage.ts`) — формат
  подогнан 1:1 под реальный внутренний отчёт заказчика (17.09.2026), не
  придуман: "Отчёт по бурению {ДД.ММ.ГГГГ}" / "Смена {N}" / пустая строка /
  "Скважина: …" / "Буровая установка: …" / "Глубина: {забой}" / "Проходка:
  {метраж смены}" / пустая строка / текст из "Что сделано за смену". Часы
  работы в это сообщение НЕ входят. Отчёт для заказчика (сводный по всем
  скважинам участка сразу) — отдельная, более крупная задача, сознательно
  отложена вторым этапом
- **`npx tsc --noEmit` не ловит всё** — этот проект собирается через `tsc -b`
  (project references, см. `npm run build`), и он строже: минимум один раз
  словил ошибку индексации `Record<TaskType, …>` значением `TaskType |
  undefined` из `useParams()`, которую `tsc --noEmit` пропустил. Перед
  коммитом — всегда `npm run build`, не только `--noEmit`
- **Работы, прицепленные к заданию на бурение (19.09.2026)** — по решению
  заказчика: если керн/распиловка/опробование ведутся на ТОЙ ЖЕ скважине,
  что и бурение (частый случай в контрактах), их больше не обязательно
  заводить отдельным заданием через "Участок" — можно прицепить прямо в
  форме правки задания на бурение (`DrillingTaskForm.tsx`, правая колонка,
  только в режиме правки — для нового задания сначала нужно его сохранить).
  Технически это ВСЁ ТЕ ЖЕ строки в `core_description_tasks`/
  `core_sawing_tasks`/`sampling_tasks` с `drilling_task_id`, что и раньше —
  никакой миграции не потребовалось, RLS не менялась. Новое — только UI:
  - `src/components/Attached{CoreDescription,Sawing,Sampling}Card.tsx` —
    компактные карточки "добавить/редактировать" внутри DrillingTaskForm.
    На скважине может быть ОБЕ документации керна одновременно (геологическая
    и геотехническая — два разных `core_description_tasks`, см. миграцию 0007).
  - **Сводка по бурению** (`DailyReportForm.tsx`, только `taskType==='drilling'`)
    подтягивает прицепленные задачи и показывает для них доп. поля прямо в
    той же форме (интервалы керна на каждую документацию, метраж распиловки,
    пробы). При сохранении это по-прежнему ОТДЕЛЬНЫЕ строки `reports` (по
    одной на прицепленную задачу, `saveAttachedReport`) с той же датой/сменой/
    часами/комментарием, что и у сводки по бурению — **согласование
    остаётся раздельным по каждому виду работ** (решение от 19.09.2026,
    техдир может одобрить бурение и отдельно отклонить, например,
    опробование той же смены). Костыль-но-нужный момент: при правке уже
    существующей сводки по бурению форма ищет "сиблингов" — строки
    прицепленных работ с той же датой/сменой — и обновляет их, а не
    дублирует; это происходит один раз при загрузке формы, не реактивно
    при смене даты в уже открытой форме (если дата меняется вручную на
    уже занятую — исходный риск дублирования тот же, что был всегда для
    опробования, см. ниже). Если сиблинг уже НЕ в статусе draft/edit_unlocked
    (техдир успел одобрить/просмотреть его отдельно от бурения) — форма
    показывает его поля только для чтения (`siblingLocked`) и не трогает эту
    строку при сохранении вообще: RLS всё равно не даст её обновить (0 строк,
    та же ошибка PGRST116, что чинили 19.09.2026 в другом месте), а вставка
    новой строки рядом вместо обновления задвоила бы метраж/пробы после
    одобрения обеих. Проверено вживую 19.09.2026 под реальным бригадиром:
    отправка объединённой сводки создаёт ровно 5 раздельных строк с общими
    датой/сменой/часами, повторное открытие на правку находит все сиблинги
    и обновляет их без дублей (одна новая строка — только для впервые
    заполненного вида работы).
  - Опробование можно вписать в объединённую форму, ТОЛЬКО если
    `assigned_party_chief_id` прицепленного задания = текущий пользователь
    (иначе `reports_insert_own` не даст записать строку от его имени) —
    по умолчанию при прицеплении подставляется бригадир бурения, но поле
    редактируемое (может быть другая бригада/лаборатория). Если
    ответственный — не бригадир бурения, тот вносит сводки по опробованию
    по-старому, через самостоятельное задание
  - Копирование в WhatsApp (`buildDrillingShiftMessage`) — доп. строки
    (керн/распиловка/пробы) добавляются ТОЛЬКО если задача прицеплена И
    что-то реально введено; без вложений формат не меняется ни на символ
    (он подогнан 1:1 под реальный отчёт заказчика, см. запись выше)
  - Затраты (`report_costs`) остаются привязаны только к строке бурения —
    не дублируются и не делятся между прицепленными работами той же смены
  - `SiteDetail.tsx` — керн/распиловка/опробование со `drilling_task_id`
    теперь рендерятся ВЛОЖЕННЫМИ в карточку своей скважины (`.attached-row-*`
    в index.css), а не отдельными списками — отдельные списки остались
    только для скважины подрядчика (`external_well_number`, нет
    `drilling_task_id`). Распиловка не может быть на скважине подрядчика
    вообще (см. запись про миграцию 0007) — для неё отдельного списка
    больше нет совсем
  - Старые самостоятельные формы создания (кнопки "+ Задание: ..." в шапке
    участка) НЕ убраны — работают как раньше, это по-прежнему единственный
    способ завести задание на скважину подрядчика. Для своей скважины
    предпочтительный путь теперь — через правку задания на бурение
  - Не реализовано сознательно (см. диалог 19.09.2026, "выполним позже"):
    аналогичное прицепление подзадач к ЗАДАНИЯМ-ЯКОРЯМ другого типа
    (тендер только на описание керна/только на опробование скважины
    подрядчика) — сейчас якорем может быть только бурение
- **Левый сворачиваемый тулбар вместо верхнего меню (19.09.2026)** —
  `TopNav.tsx` заменён на `src/components/layout/Sidebar.tsx` (только ПК,
  >=768px, см. `.sidebar` в `index.css`). Свёрнутое/развёрнутое состояние —
  `localStorage` (`gqs-sidebar-collapsed`), не БД: это предпочтение
  конкретного браузера, не общий параметр проекта. `NAV_ITEMS` (единый
  источник для сайдбара/мобильного таб-бара) не менялся по структуре — просто
  рендерится вертикально. Общие классы `.brand`/`.brand-mark` (лого) теперь
  используются и сайдбаром, и `MobileHeader.tsx` — не дублировать под новым
  именем. `UserMenu.tsx` принимает `placement="up"` (сайдбар открывает меню
  вверх, снизу экрана — вниз было бы за пределами вьюпорта) и
  `showLabel` (в развёрнутом сайдбаре показывает имя/роль рядом с аватаром)
- **Кнопка "Сводка" на карточке участка (19.09.2026)** — раньше вела сразу на
  создание новой сводки (`/reports/new`), теперь открывает список
  ("историю") сводок задания (`/reports`) — создание сводки осталось
  доступно из самого списка (кнопка "Новая сводка") и через отдельную
  одноимённую кнопку внутри `TaskDashboard.tsx`, которая по-прежнему ведёт
  на `/reports/new`. Два визуально похожих места, разное поведение — так и
  задумано (список — обзор, дашборд задания — быстрое добавление)
- **"Мои сводки" — новый экран для бригадира (19.09.2026)**,
  `src/routes/reports/MyReports.tsx`, роут `/reports/mine`, пункт меню
  только для `party_chief`. Решает то, что список сводок ОДНОГО задания
  (`TaskReportsList.tsx`) в принципе не может показать: сквозную историю
  сразу по всем заданиям бригадира (все скважины, все виды работ).
  Категории — по факту (задание, у которого есть хотя бы одна своя сводка),
  не по заданиям без сводок. Категория при клике открывает модалку
  (`.modal-backdrop`/`.modal-panel` в index.css — переиспользовать для
  будущих модалок) с постраничным списком (`PAGE_SIZE = 3`) и ссылкой
  "Открыть полный список" на тот же `TaskReportsList.tsx`. Данные о своих
  заданиях (`drilling_tasks`/`core_description_tasks`/...) не фильтруются
  на фронте по бригадиру — RLS уже отдаёт только "свои" (`foreman_id`/
  `assigned_party_chief_id = auth.uid()`)
- **Двойной сабмит сводки (19.09.2026)** — `DailyReportForm.tsx` `handleSubmit`
  защищён синхронным `useRef`-флагом (`submittingRef`), а не только
  `disabled={submitting !== null}` на кнопке. Реальный баг: быстрый повторный
  тап (характерно на телефоне при пересдаче отклонённой сводки) успевает
  вызвать `handleSubmit` дважды ДО того, как React перерисует `disabled` —
  React-стейт обновляется асинхронно. Первый вызов доводит сводку до
  `submitted` (снимает `edit_unlocked`), второй параллельный вызов на этапе
  черновика делает `update().select().single()` по уже неэдитируемой (по RLS)
  строке → 0 строк → PostgREST `PGRST116` `"Cannot coerce the result to a
  single JSON object"`. Если увидите этот текст ошибки где-то ещё в проекте —
  ищите такой же паттерн (async-обработчик без синхронной защиты от повторного
  вызова), не RLS-политику
- **Правка заданий (17.09.2026)** — все 4 формы заданий (`*TaskForm.tsx`)
  теперь двойного назначения: `taskId` в URL (`/sites/:siteId/tasks/:type/:taskId/edit`)
  включает режим правки той же формой, что и создание — insert/update
  собираются как ДВА раздельных вызова с разными объектами полей (`status`
  только при update, `created_by` только при insert), не один объект с
  условным спредом в один `insert/update` через тернарник — так ловилась
  ошибка типов на границе union (`tsc -b` ловит, `--noEmit` иногда нет, см.
  выше). Диаметры при правке — снести старые строки и записать текущее
  состояние формы заново (тот же приём, что с затратами сводки)
- **Двухуровневые статьи затрат (19.09.2026)** — раньше `cost_categories`
  была плоским списком ("ГСМ", "Коронки", "Материалы (прочее)"), единица
  измерения была на самой категории, а сам справочник правился только через
  SQL/Table Editor. По запросу заказчика (тестировал сводку на телефоне,
  захотел сам добавлять статьи) сделано два уровня: категория (просто
  название) → виды затрат внутри неё (название + единица измерения),
  миграция `0008_cost_items.sql`, новая таблица `cost_items` с
  `category_id`. `report_costs` теперь ссылается на `cost_item_id`
  (было `cost_category_id`) — колонка `unit` с `cost_categories` перенесена
  на `cost_items`. Старые 3 категории переносятся 1:1 в виды затрат тем же
  названием/единицей, чтобы существующие `report_costs` не потерялись.
  - `src/routes/settings/CostCategoriesSettings.tsx` — экран управления,
    доступен только management (как "Пользователи"), пункт меню "Статьи
    затрат". "+ Добавить категорию" наверху, у каждой категории — карточка
    со списком видов и своей мини-формой "+ Добавить вид затрат" (имя +
    единица измерения: выбор из частых вариантов шт/л/м/м³/т/кг/компл. или
    "другая…" с ручным вводом) — по образцу `Attached*Card.tsx` (сама
    пишет в базу, сообщает наверх колбэком, без перезагрузки списка)
  - `CostRowsEditor.tsx` (используется в `DailyReportForm.tsx`) — выбор
    затрат в сводке стал каскадным: сначала категория, затем вид затрат
    внутри неё (список видов фильтруется на клиенте по `category_id`)
  - `ReportDetail.tsx`/`ReportReview.tsx`/`SummaryReport.tsx` — везде, где
    раньше показывалось название категории у строки затрат, теперь
    показывается `"Категория — Вид"` (join через `cost_items.category_id`)
  - **Важно**: миграция `0008` ДРОПАЕТ `cost_categories.unit` и
    `report_costs.cost_category_id` — до применения владельцем через SQL
    Editor приложение продолжает работать на старой схеме без ошибок
    (запрос к ещё не существующей `cost_items` просто возвращает пустой
    список, дропдаун "вид затрат" остаётся пустым), но добавить затраты в
    сводке нельзя, пока миграция не накатана

- **Организации бурения, буровые станки, работники (19.09.2026)** — по
  запросу заказчика во время создания задания на бурение: раньше номер
  станка был свободным текстом, организации/работники правились только
  через SQL. Миграция `0009_drilling_rigs_and_workers.sql`.
  - **Буровые станки** — новая таблица `drilling_rigs` (номер, марка/модель,
    вид бурения — колонковое/шнековое/роторное/ударно-канатное или свой
    вариант). Станок **навсегда закреплён за одной организацией**
    (`organization_id`, своей или подрядчика) — решение заказчика, передача
    между организациями не предусмотрена. `drilling_tasks.rig_number`
    (свободный текст) заменён на `drilling_rig_id` (FK) — старые значения
    переносятся 1:1 в справочник при миграции, привязка к заданию
    сохраняется. `src/routes/tasks/DrillingTaskForm.tsx` — выбор каскадный:
    сначала организация, потом станок внутри нее (список фильтруется на
    клиенте по `organization_id`, сбрасывается при смене организации)
  - **Организации бурения** — экран
    `src/routes/settings/DrillingOrganizationsSettings.tsx` (management
    only, пункт меню "Организации бурения"): "+ Добавить организацию"
    наверху (новые всегда `is_own=false`, "своей" может быть только та,
    что отмечена в БД — обычно ровно одна), у каждой организации —
    раскрывающаяся карточка со станками внутри и формой "+ Добавить
    станок" — тот же UI-паттерн, что и в `CostCategoriesSettings.tsx`
    (категория → виды затрат)
  - **Работники** — новая сущность `workers`, **сознательно НЕ связана с
    `profiles`/`auth.users`** (решение заказчика: это справочник состава
    бригады для учёта, а не логин-аккаунты, в отличие от бригадира
    `party_chief`). Поля: ФИО, должность (свободный текст), организация,
    `assigned_foreman_id` (ссылка на `profiles` с ролью `party_chief`) —
    **текущая** привязка к бригадиру, истории смен не ведём (сознательно
    просто, при необходимости бригадир меняется вручную в любой момент).
    Экран `src/routes/settings/WorkersSettings.tsx` (management only,
    пункт меню "Работники"): форма добавления + список карточек с
    фильтрами по организации/бригадиру и инлайн-select смены бригадира
    прямо на карточке (сохраняется сразу при выборе, без отдельной кнопки
    "сохранить") — это и есть "распределение работников по бригадирам"
  - RLS у всех трёх новых/изменённых таблиц — как у остальных справочников
    (`cost_categories` и т.п.): SELECT всем авторизованным, INSERT/UPDATE/
    DELETE только `is_management()`
  - Баг, пойманный вживую при тестировании под гендиром: раскрывающая
    кнопка категории/организации (`background: transparent`) наследовала
    глобальный `color: #fff` от базового стиля `button` в `index.css` —
    текст названия был белым по белому/кремовому фону, невидимым, хотя в
    DOM он был. Чинится явным `color: var(--color-text)` на самой кнопке
    — если где-то ещё в проекте появится прозрачная кнопка поверх
    `<button>`, нужно так же не забывать явно задавать цвет текста, не
    полагаться на дефолт

- **Формы добавления — только в модалке (19.09.2026)** — по отзыву
  заказчика: раньше формы "добавить X" либо всегда были развёрнуты наверху
  списка (Участки, Пользователи, Работники, категории/организации), либо
  появлялись инлайн внутри карточки при клике (виды затрат внутри
  категории, станки внутри организации) — хотелось единообразия и чтобы
  форма не терялась среди остального контента. Теперь везде: кнопка
  "+ Добавить …" (у страниц — справа от заголовка `<h1>`, у карточек —
  внутри развёрнутого блока) открывает `src/components/Modal.tsx` —
  переиспользуемый компонент (заблюренный/затемнённый фон на весь экран,
  закрытие по клику на фон или крестик), тот же визуальный паттерн, что
  уже был в `MyReports.tsx` для просмотра истории сводок (`.modal-backdrop`/
  `.modal-panel` в `index.css`, теперь `.modal-backdrop` ещё и с
  `backdrop-filter: blur(4px)`). Затронуты: `SitesList.tsx`,
  `UsersList.tsx`, `CostCategoriesSettings.tsx` (и категория, и вложенный
  вид затрат), `DrillingOrganizationsSettings.tsx` (и организация, и
  вложенный станок), `WorkersSettings.tsx`. Форма при успехе обычно
  закрывает модалку и сбрасывает поля; ошибка/успех показываются ВНУТРИ
  модалки (не на странице позади неё, иначе не видно). У `UsersList.tsx`
  модалка НЕ закрывается автоматически при успехе — сообщение
  "пользователь создан, сообщите пароль лично" должно остаться на экране,
  пока не закроют вручную (было так и раньше, просто в развёрнутой форме)

- **Распределение работников по задачам (20.09.2026, по запросу
  заказчика)** — миграция `0010_task_worker_assignments.sql`, новая
  таблица `task_worker_assignments`. Раньше "работники" (миграция 0009)
  были привязаны только к бригадиру ("своя бригада"), но не к конкретному
  заданию/скважине. Теперь бригадир (или тот, кто назначен ответственным
  за конкретное задание — `assigned_party_chief_id` на скважине
  подрядчика) может расписать, КТО из его бригады выполняет какую роль на
  ЭТОЙ конкретной скважине/задании: буровик и помощник(и) бурильщика — на
  бурении (может быть несколько помбуров сразу, буровик тоже технически не
  ограничен числом, хотя обычно один), ответственный исполнитель — на
  описании керна/распиловке/опробовании. **Это чисто учётная информация**
  (кто по факту делал) — НЕ меняет, кто согласовывает сводки и кто может
  их подавать (тот механизм — `foreman_id`/`assigned_party_chief_id` на
  самих таблицах заданий — не тронут). В частности, по распиловке
  (см. запись про решение 19.09.2026 — согласовывает всегда бригадир
  бурения) теперь ДОПОЛНИТЕЛЬНО можно отметить конкретного
  работника-исполнителя, это не отменяет то правило.
  - Работника можно выбрать ТОЛЬКО из бригады того, кто отвечает за задание
    (`workers.assigned_foreman_id` = бригадир скважины/ответственный) — и
    это проверяется не только в UI, но и в RLS (`with check` в
    `task_worker_assignments_own_foreman`), т.к. вся защита данных в
    проекте — через RLS, не через сокрытие кнопок
  - **Право редактировать распределение — не только у management.**
    Бригадир (или ответственный за скважину подрядчика) тоже может сам
    распределять СВОИХ работников по СВОИМ заданиям — RLS-политика
    `task_worker_assignments_own_foreman` смоделирована 1:1 по структуре
    `reports_insert_own` (EXISTS по каждому из 4 видов заданий, тот же
    принцип владения). Это № первый случай в проекте, где party_chief
    получает право писать НЕ в `reports`/`report_costs`, а в
    "родительскую" сущность задания — но только в отдельную таблицу
    `task_worker_assignments`, не в сами `drilling_tasks` и т.п. (та форма
    редактирования задания остаётся management-only, см. запись про
    `DrillingTaskForm.tsx` выше)
  - UI — новый компонент `src/components/CrewAssignmentSection.tsx`,
    встроен в `TaskDashboard.tsx` (общий дашборд для всех 4 видов заданий,
    туда и так уже заходит и party_chief на свои задания, и management —
    так избежали дублирования прав доступа в отдельной форме). Показывает
    роли для этого вида задания (`ROLE_SLOTS`), список уже назначенных
    работников чипами с крестиком (удалить) и, если есть право
    редактировать, select "+ добавить работника" из бригады. Право
    редактировать = `isManagement() || foreman?.id === profile?.id`, где
    `foreman` — это уже загруженный в `TaskDashboard` "ответственный за
    задание" профиль (там это поле исторически называется `foreman` для
    всех 4 видов, включая `assigned_party_chief_id` внешних заданий)
  - Уникальность (задание, роль, работник) — через 4 частичных уникальных
    индекса (по одному на вид задания), а не один `UNIQUE` по всем 4
    FK-колонкам сразу: обычный составной `UNIQUE` не сработал бы, так как
    в Postgres `NULL` никогда не равен `NULL`, а у каждой строки 3 из 4
    FK-колонок всегда `NULL` (см. `one_task_source`) — единый `UNIQUE`
    просто никогда бы не сработал. Та же ловушка, что и с индексами на
    `reports`, если там когда-нибудь понадобится аналогичная уникальность
  - **Модалка "Состав бригады" вместо инлайн-блока (20.09.2026)** — по
    отзыву заказчика: кнопка "Состав бригады" теперь в шапке дашборда
    задания (`TaskDashboard.tsx`), справа от названия скважины (`h1` с
    `flex: 1` подвигает кнопку в крайнее правое положение). Открывает
    `Modal` с одной или несколькими секциями `CrewAssignmentSection`
    сразу — если это дашборд БУРЕНИЯ, внутри ОДНОЙ модалки показываются
    ВСЕ применимые секции: само бурение (буровик/помбур) + КАЖДАЯ
    прицепленная к этой скважине работа (геологическая/геотехническая
    документация керна, распиловка, опробование — грузятся тем же
    запросом по `drilling_task_id`, что в `DrillingTaskForm.tsx`), у
    каждой своя подпись (`heading` проп) и свой `canEdit`/`foremanId`
    (для опробования и внешне назначенного описания керна ответственный
    может отличаться от бригадира бурения — `assigned_party_chief_id`
    задания, если задан, иначе бригадир). Это решает жалобу "не могу
    найти, где назначать исполнителя по доп. заданиям" — раньше пришлось
    бы отдельно открывать дашборд каждого прицепленного задания. Дашборды
    остальных 3 видов заданий (когда открыты НЕ как прицепленные, а
    напрямую — например, скважина подрядчика) по-прежнему показывают
    свою кнопку/модалку с одной секцией на себя же — компонент
    `CrewAssignmentSection` от этого не изменился, только вызывающий код
    в `TaskDashboard.tsx` решает, сколько секций показать разом

- **Название участка/скважины в сводке (20.09.2026, по отзыву заказчика)**
  — на согласовании (`ReportReview.tsx`) и в просмотре сводки автором
  (`ReportDetail.tsx`) не было видно, к какой скважине/участку относится
  сводка (было только в списке "На согласовании" ДО клика, а после —
  терялось). Добавлена общая функция `src/lib/reportLabel.ts` →
  `loadReportSiteAndWellLabel(report)` — по одной сводке достаёт название
  участка + подпись задания (тип работы + своя/подрядчика скважина + №),
  используется в обоих местах. Заодно найден и починен смежный баг в
  `PendingApprovals.tsx` (список "На согласовании"): номер скважины у
  сводок по ПРИЦЕПЛЕННЫМ подзаданиям (керн/распиловка/опробование) не
  подтягивался вообще (показывал "?"), потому что `drilling_task_id` у
  таких сводок лежит не в `reports.drilling_task_id` (там `null`), а на
  уровень глубже — в самой подзадаче (`core_description_tasks.
  drilling_task_id` и т.п.). Список `drilling_tasks` для подгрузки номеров
  теперь запрашивается ВТОРЫМ шагом, после того как загружены сами
  подзадачи и стали известны их вложенные `drilling_task_id`
- **Удаление сводок (20.09.2026, по запросу заказчика)** — миграция
  `0011_reports_delete.sql`. На `reports` вообще не было DELETE-политики
  (см. CLAUDE.md, было явно зафиксировано ограничение) — теперь автор
  может удалить СВОЙ черновик (`approval_status = 'draft'`, отправленную/
  согласованную/отклонённую так удалить нельзя — это уже история),
  management — сводку в любом статусе (уборка тестовых/ошибочных данных).
  Кнопка "Удалить черновик"/"Удалить сводку" — в `ReportDetail.tsx`, рядом
  с кнопкой редактирования, с двухшаговым подтверждением прямо на странице
  (не `window.confirm()` — в проекте так нигде не делали, держим
  единообразный стиль). **Важная ловушка, поймана вживую при тестировании**:
  `supabase.from('reports').delete().eq('id', id)` без RLS-политики,
  разрешающей удаление, возвращает `error: null` и просто не трогает ни
  одной строки — БЕЗ `.select()` после `.delete()` это выглядит как
  успех (код думает, что удалил, и уходит со страницы), хотя на деле
  строка осталась в базе. Обязательно проверять `data`/`deletedRows` на
  пустоту, а не только `error`, после `delete()` — актуально для ЛЮБОГО
  будущего `delete()` в проекте, не только этого

- **Категоризация "Сводок на согласовании" (20.09.2026, по запросу
  заказчика)** — `PendingApprovals.tsx` был плоским списком, отсортированным
  только по времени отправки; при нескольких участках сразу было сложно
  ориентироваться. Теперь трёхуровневая группировка: **участок → скважина/
  задание → вид работ под заданием** (`groupReports()`), плюс 3 фильтра
  сверху — по участку, по заданию (сужается каскадно под выбранный
  участок) и по бригадиру (по автору сводки). Участки — раскрывающиеся
  карточки (chevron, тот же паттерн, что в `CostCategoriesSettings.tsx` и
  т.п.), по умолчанию все развёрнуты; скважина и вид работ внутри —
  просто визуальная группировка, не сворачиваются (обычно там всего
  несколько строк, доп. клики не нужны). Ключ группировки "скважина" —
  `drilling_task_id`, ОБЩИЙ для бурения и всех прицепленных к нему работ
  (см. отзыв 19.09.2026 про прицепленные подзадачи) — поэтому сводка по
  бурению и сводка по геологической документации той же скважины
  попадают в одну группу; для скважины подрядчика (нет `drilling_task_id`)
  используется синтетический ключ на саму подзадачу
- **Синхронизирована с "Мои сводки"** идея группировки, но здесь
  дополнительно есть фильтры (в `MyReports.tsx` их нет — там и так уже
  разбито по заданиям бригадира персонально)

- **Забой-подсказка и автосброс формы после отправки (20.09.2026, по
  запросу заказчика)** — `DailyReportForm.tsx`. Раньше кнопка "Отправить
  на согласование" оставалась активной и после успешной отправки: сама
  сводка при этом уже "ушла" на согласование (author больше не может её
  update'ить — RLS), но форма с теми же значениями оставалась на экране,
  и повторный клик на новую сводку (не правку) реально создал бы ВТОРУЮ
  строку `reports` на ту же дату/смену (`isEditMode` не пересчитывается
  после отправки в рамках одной сессии формы, а `.insert()` не проверяет
  "может уже есть такая").
  - Теперь после успешного "Отправить на согласование" **для новой
    сводки** (`!isEditMode`): форма сама очищает все поля (метраж, керн,
    распиловка, пробы, комментарий, статьи затрат, поля прицепленных
    работ) и сбрасывает `siblingIds`/`siblingLocked` — важно, иначе при
    смене даты новые прицепленные строки обновляли бы старые, уже
    отправленные. Дата/смена НЕ сбрасываются специально — на них держится
    защита от повторной отправки (см. ниже). **Для правки** (пересдача
    отклонённой/уже отправленной, `isEditMode`) — как и раньше при
    сохранении черновика, уходим со страницы на список сводок: остаться
    было бы одинаково рискованно (тот же reportId, второй клик упёрся бы
    в PGRST116), но "начать новую смену" тут не имеет смысла
  - Кнопки "Сохранить черновик"/"Отправить на согласование" блокируются,
    пока дата+смена в форме совпадают с той, что только что была успешно
    отправлена в этом сеансе (`lastSubmitted` state) — кнопка сама меняет
    подпись на "Уже отправлено — измените дату/смену". Смена даты ИЛИ
    смены снимает блокировку реактивно, без перезагрузки страницы —
    проверено вживую (отправил смену 1 за 21.09 → кнопка блокируется →
    сменил дату на 22.09 → разблокировалась сама)
  - **Забой-подсказка возле "Метраж бурения за смену"** — раньше
    показывала только `priorApprovedMeters` (сумма ТОЛЬКО согласованных
    смен, используется и в WhatsApp-сообщении, там принципиально не
    менялось). Теперь рядом — `knownMeters` (согласованные + ещё не
    согласованные `submitted`), обновляется сразу локально после
    собственной отправки, не дожидаясь техдира ("для понимания", по
    формулировке заказчика). Если `knownMeters !== priorApprovedMeters`
    — рядом красная точка + текст "не согласован" (только на этот экран,
    WhatsApp не трогаем)

- **Переименование существующих статей затрат (20.09.2026, по запросу
  заказчика)** — `CostCategoriesSettings.tsx` раньше умел только
  ДОБАВЛЯТЬ категории/виды затрат, не редактировать уже созданные
  (заказчик успел завести "ГСМ" и хотел переименовать в "Солярка" без
  пересоздания). Добавлена кнопка-карандаш у каждой категории (в шапке
  карточки) и у каждого вида затрат внутри неё (в строке списка) — обе
  открывают ту же модалку `Modal`, что и добавление, только предзаполненную
  текущими значениями и делающую `.update()` вместо `.insert()`. Для вида
  затрат общие поля (имя + селект единицы измерения) вынесены в
  `ItemFields` — переиспользуются формой добавления и формой
  редактирования, чтобы не дублировать разметку. `unitToSelectValue()`
  корректно подставляет "другая…" в селект, если у существующего вида
  единица измерения не входит в стандартный список (заведена вручную
  раньше) — иначе редактирование молча откатило бы её на первый вариант
  из списка. Технически важно: заголовок категории раньше был ОДНИМ
  `<button>` на всю ширину (клик разворачивал список) — кнопку-карандаш
  внутрь такого же `<button>` вложить нельзя (невалидный HTML, вложенные
  `<button>`), поэтому шапку карточки переструктурировали на `<div>` с
  двумя соседними `<button>` внутри (переключатель разворачивания и
  переименование) вместо одного составного

- **"Мои сводки" переведён на ту же группировку, что "Согласование"
  (20.09.2026, по запросу заказчика)** — бригадир пожаловался, что экран
  выглядит "как беспорядок: просто значок бурения и количество сводок,
  никакой категоризации". Раньше это был плоский грид карточек "иконка +
  название скважины + счётчики", отсортированный по дате последней
  сводки — без привязки к участку вообще (участок нигде не показывался),
  и при нескольких видах работ на одной скважине карточки выглядели
  разрозненно. Теперь та же трёхуровневая группировка участок → скважина
  → вид работ, что и в `PendingApprovals.tsx` (тот же визуальный паттерн:
  раскрывающиеся карточки участков с chevron, скважина — подзаголовок с
  границей, вид работ — плитка с иконкой/подписью/счётчиками, клик
  открывает ту же модалку с постраничным списком, что была и раньше —
  сам механизм модалки не менялся). Понадобился доп. запрос к `sites`
  (раньше участок вообще не подгружался на этом экране).
  - **Важная находка при живой проверке**: то, что бригадир описал как
    "просто значок и число, без текста" — оказалось НЕ описанием старого
    дизайна как такового, а тем же багом невидимого текста, что уже
    чинили в `CostCategoriesSettings.tsx`/`DrillingOrganizationsSettings.tsx`
    19.09.2026: `<button className="card card-interactive">` без явного
    `color` наследует глобальный `button { color: #fff }` — белый текст
    на светлом фоне карточки, физически присутствует в DOM (проверяется
    через `read_page`/accessibility-дерево), но не виден на экране. Этот
    баг был и в САМОЙ ПЕРВОЙ версии `MyReports.tsx` (19.09.2026) с самого
    начала — просто раньше никто не пожаловался. **Правило на будущее**:
    у ЛЮБОГО `<button>` с классом `.card`/`.card-interactive` (у них
    самих `color` не задан) в проекте ОБЯЗАТЕЛЬНО ставить
    `color: 'var(--color-text)'` явно в inline-style — иначе текст будет
    невидимым. Для `<Link>` (рендерится как `<a>`) эта проблема не
    возникает, т.к. `button { color: #fff }` на `<a>` не действует —
    поэтому, например, строки сводок в модалке (`<Link className="card
    card-interactive">`) всегда были видны нормально

- **Роль "разработчик" — скрытый суперпользователь (22.09.2026, по запросу
  владельца платформы)** — миграция `0012_developer_role.sql`. Владелец
  хочет быть НЕ гендиром, а отдельной ролью с ПОЛНОЙ властью над
  платформой (всё, что может гендир/техдир, и сверх того), но при этом
  быть "серым кардиналом" — невидимым для гендира/техдира нигде, включая
  список "Пользователи".
  - `is_management()` теперь возвращает true и для `role = 'developer'`
    — так разработчик автоматически наследует ВСЕ существующие проверки
    `is_management()` по всему проекту (участки, задания, согласование,
    все справочники, оргструктура) без правки уже написанных политик.
    Отдельная `is_developer()` — только для того, что относится
    ИСКЛЮЧИТЕЛЬНО к разработчику (сейчас — снятие ограничения видимости
    себя же).
  - **Невидимость — через RLS, не через сокрытие UI**: `profiles_select_
    own_or_management` переписана так, что гендир/техдир видят все
    профили КРОМЕ `role='developer'` (разработчик видит всех, включая
    себя). `profiles_insert_management`/`profiles_update_management`
    дополнительно требуют `role <> 'developer' or is_developer()` — то
    есть создать или изменить профиль-разработчик может только сам
    разработчик, гендир не может этого сделать в обход UI напрямую через
    API (закрыт и "слепой" UPDATE без SELECT-доступа к строке).
  - `src/types/roles.ts`: `UserRole` пополнен значением `'developer'`,
    `isManagement()` признаёт и его. **Сознательно НЕ добавлено** в
    `ROLE_OPTIONS` формы создания пользователя (`UsersList.tsx`) — через
    интерфейс создать разработчика нельзя в принципе, только вручную
    через SQL (см. инструкцию ниже, аналогично бутстрапу самого первого
    гендира в `supabase/README.md`)
  - Учётку разработчика заводит владелец САМ: Supabase Dashboard →
    Authentication → Users → Add user (email + пароль), затем в SQL
    Editor: `insert into profiles (id, full_name, role) values ('<UID>',
    'Имя', 'developer');` — тот же приём, что для первого гендира, но
    с ролью `developer`
- **Оргструктура (22.09.2026, по запросу владельца платформы)** —
  миграция `0013_org_positions.sql`, новая таблица `org_positions` +
  экран `src/routes/org/OrgChart.tsx`, пункт меню "Оргструктура" (виден
  всем ролям — читать может любой авторизованный, редактирует только
  management, включая разработчика).
  - **Дерево должностей** (`parent_id`, самоссылка) — НЕ абстрактный
    справочник названий, а конкретные штатные единицы: "Мастер"
    встречается в схеме несколько раз в разных ветках как независимые
    узлы (ровно как на присланной заказчиком картинке оргструктуры).
    Рисуется чистым CSS-деревом на `<ul>/<li>` с линиями-коннекторами
    через `::before`/`::after` (см. `.org-tree*` в `index.css`) — без
    сторонних библиотек диаграмм, т.к. глубина/ветвление небольшие
  - **Назначение человека на должность — через `assigned_worker_id`
    (справочник `workers`)**, СОЗНАТЕЛЬНО не через `profiles` — заказчик
    предпочёл не вести два параллельных реестра людей, "Работники" и так
    уже используются для распределения по буровым бригадам
    (`task_worker_assignments`, миграция 0010). Реальный логин для
    ролей, которые реально заходят в систему (гендир/техдир/бригадир),
    заводится отдельно как обычно, через "Пользователи" — оргструктура
    с этим механизмом напрямую не связана, это скорее визуальная карта
    штата, чем источник прав доступа
  - **`submits_reports`** — булев флаг на узле (на присланной схеме это
    три "Мастер" в оранжевой рамке): помечает, что держатель ЭТОЙ
    должности реально работает в системе и отправляет сводки. Чисто
    визуальная метка (оранжевая рамка `.org-node-flagged`), никакой
    автоматической связи с `profiles`/RLS не создаёт — на будущее, если
    понадобится реально связать конкретный узел оргструктуры с конкретным
    логин-аккаунтом, это отдельная задача
  - **Права на структуру** — по решению заказчика добавлять/удалять/
    переименовывать должности и менять иерархию (родителя) может любой
    management (гендир/техдир/разработчик), не только разработчик —
    единая RLS-политика `org_positions_write_management` на `is_management()`
  - Перенос должности в другую ветку (смена `parent_id`) — select с
    родителем на форме редактирования узла ИСКЛЮЧАЕТ саму должность и
    ВСЕХ её потомков (`collectDescendantIds()`), иначе дерево можно было
    бы зациклить (сделать узел своим же дедушкой). Проверки цикла на
    уровне БД нет (для организации в 15-20 узлов сочли избыточным) —
    защита только на уровне UI, но им пользуются только гендир/техдир/
    разработчик
  - Удаление узла — `on delete cascade` сносит и всё поддерево; форма
    предупреждает точным числом ("вместе с N подчинённой(ыми)
    должностью(ями)") перед подтверждением (двухшаговое, как и удаление
    сводок 20.09.2026 — без `window.confirm()`)
  - Сид (в этой же миграции) — ровно та схема, что прислал заказчик:
    Генеральный директор → Технический директор → [Главный геотехник:
    Ведущий инженер-геотехник/Инженер-геотехник (у каждого свой "Мастер",
    submits_reports) + Ведущий-геолог/Геолог (у каждого свой
    Техник-геолог)] и [Начальник буровой партии → Мастер (submits_reports)
    → Машинист буровой установки (→ Помощник машиниста) + Водитель
    Урал-5557]. Все узлы без назначенного работника — заказчик заполнит
    сам по мере найма/миграции реальных данных
- **Отложено по прямому указанию заказчика (22.09.2026)**: полная очистка
  данных участков (`sites` + все 4 вида заданий + `reports`/`report_costs`/
  `task_worker_assignments`) для начала миграции на реальные данные —
  заказчик попросил НЕ выполнять сразу, уточнит точный объём отдельным
  запросом перед выполнением. Не делать это самостоятельно без явного
  повторного подтверждения — действие необратимо, бэкапа нет

- **Архивирование и удаление работников, удаление пользователей
  (23.09.2026, по запросу владельца платформы)** — миграция
  `0014_workers_archive_and_deletes.sql`.
  - `workers.archived_at` (nullable timestamptz) — мягкое скрытие, НЕ
    удаление: у работника может быть история (`task_worker_assignments`,
    `org_positions.assigned_worker_id`), терять её незачем. Экран
    `WorkersSettings.tsx` — чекбокс "показывать архивных" (по умолчанию
    выключен, архивные скрыты из основного списка), у каждой карточки
    кнопки "Архивировать"/"Вернуть из архива" и "Удалить" (двухшаговое
    подтверждение). `CrewAssignmentSection.tsx`/`OrgChart.tsx` — архивных
    работников нельзя НАЗНАЧИТЬ заново (убраны из выпадающих списков
    выбора), но уже существующие назначения на архивного работника
    продолжают показывать его имя как раньше (не переиспользуют
    отфильтрованный список для резолва имени, только для списка "доступны
    к выбору") — иначе история молча "обезличилась" бы
  - **Удаление работника** — для DELETE отдельная RLS-политика не
    потребовалась: `workers_write_management` уже была `for all`,
    покрывает и удаление. `task_worker_assignments.worker_id` —
    `on delete cascade` (снесёт и записи о распределении), `org_positions.
    assigned_worker_id` — `on delete set null` (просто освободит
    должность) — предупреждение в UI перед удалением написано с учётом
    обоих случаев
  - **Удаление пользователя** (`profiles`) — RLS-политики на DELETE не
    было вообще (тот же пробел, что чинили у `reports` в 0011). Новая
    политика `profiles_delete_management`: может management, НЕ самого
    себя (защита от случайной потери единственного гендира) и НЕ профиль
    `role='developer'`, если сам не разработчик (тот же принцип
    неприкосновенности, что у insert/update в 0012). Удаляется ТОЛЬКО
    строка `profiles` — саму учётную запись в Supabase Auth с фронтенда
    удалить нельзя (нет service_role/admin API, см. архитектурное решение
    в самом начале этого файла) — без профиля пользователь фактически
    полностью теряет доступ к приложению через RLS, хоть формально Auth-
    аккаунт и остаётся. Кнопка "Удалить" в `UsersList.tsx` скрыта для
    своей же карточки. Если у пользователя есть история (`author_id`,
    `foreman_id`, `created_by`, `approved_by`, `assigned_party_chief_id`,
    `workers.assigned_foreman_id` — ни у одного из этих FK на `profiles`
    нет каскада) — Postgres вернёт `foreign_key_violation` (код `23503`),
    в UI это дружелюбное сообщение "нельзя удалить — есть сводки/
    назначения, сначала переназначьте". Это осознанное поведение
    (защита от потери истории), не баг
- **Разовая очистка тестовых данных участков (23.09.2026)** — по
  прямому запросу владельца после того, как реальная миграция данных
  началась. Выполнено НЕ как функция приложения, а разовым SQL-скриптом
  (`delete from sites;`) — вся цепочка `drilling_tasks`/
  `core_description_tasks`/`core_sawing_tasks`/`sampling_tasks`/
  `reports`/`report_costs`/`task_worker_assignments` увязана каскадами
  на `sites` (кроме `reports.site_id`, у которого каскада нет — но это
  не проблема: строка `reports` в любом случае удаляется раньше через
  каскад от своего task-FK, `one_task_source` гарантирует ровно один из
  4 FK всегда заполнен). Пользователи, работники, организации/станки,
  статьи затрат, оргструктура — не участок-специфичные, не затронуты

- **Оргструктура: назначение из пользователей, не только из работников
  (24.09.2026, обнаружено при реальном использовании)** — миграция
  `0015_org_positions_profile_assignment.sql`. Изначально должность можно
  было назначить ТОЛЬКО из справочника `workers` (решение 22.09.2026) —
  на практике оказалось неудобно для позиций, которые и так уже реальные
  логин-пользователи (Генеральный директор/Технический директор/
  Начальник буровой партии): человек уже есть в БД как `profiles`, а
  выбрать его в схеме было нельзя, дублировать в `workers` не хотелось.
  - `org_positions.assigned_profile_id` (доп. к `assigned_worker_id`) —
    ровно один из двух или ни одного (`check num_nonnulls(...) <= 1`,
    тот же приём, что и everywhere в проекте для "один из N"). `on delete
    set null` — если пользователя удалят, должность просто освобождается,
    а не блокирует удаление (в отличие от прямых `foreign_key` на
    `profiles` без каскада в других таблицах — см. запись про удаление
    пользователей выше, там это осознанно наоборот)
  - `OrgChart.tsx` — select назначения объединяет ОБА источника с
    `<optgroup>` ("Пользователи (вход в систему)" / "Работники"),
    значения различаются префиксом (`profile:<uuid>`/`worker:<uuid>`,
    сами по себе UUID из разных таблиц неразличимы). **Разработчик
    исключается из списка пользователей ЯВНЫМ фильтром
    `.neq('role','developer')`**, а не только RLS — если схему
    редактирует сам разработчик, RLS пропустил бы его собственный
    профиль (id = auth.uid() всегда проходит select-политику), и он мог
    бы случайно назначить сам себя на видимую должность, нарушив
    собственное требование "нигде не отображаться"

- **Оргструктура: мобильная адаптация без смены концепции (25.09.2026)**
  — первая попытка (в этот же день) заменить графовое дерево с
  линиями-коннекторами на раскрывающийся отступный список ради мобильной
  адаптации была ОТКАЧЕНА обратно по прямому отзыву владельца платформы:
  "тот вариант больше нравился, он был удобен, просто немного не
  адаптирован". `OrgChart.tsx` возвращён к исходной версии графа
  (`git checkout HEAD --`, диффа не осталось). Адаптация под телефон
  сделана ИНАЧЕ — не сменой структуры, а компактностью внутри той же
  структуры: `@media (max-width: 767px)` в `index.css` уменьшает узлы/
  коннекторы/шрифт, добавлен `-webkit-overflow-scrolling: touch`.
  Центрирование (`justify-content: center` на `.org-tree`/вложенных `ul`)
  сознательно НЕ убрано в мобильной версии — пробовали заменить на
  `flex-start` (выравнивание по левому краю), стало хуже: браузер
  переставал сам скроллить к центру дерева при overflow, первый экран на
  телефоне показывал пустоту слева от ствола. **Урок на будущее**: для
  этой техники (центрированное CSS-дерево с коннекторами через
  `::before`/`::after`) выравнивание по краю — не "более мобильный"
  вариант, он ломает единственную причину, по которой браузер сам находит
  разумную стартовую точку прокрутки
- **Нижний таб-бар: overflow "Ещё" (25.09.2026)** — тем же заходом
  найден реальный баг: `BottomTabBar.tsx` рендерил ВСЕ пункты меню
  management-роли (9 штук) через `grid-auto-columns: 1fr` на 375px экране
  — подписи наезжали друг на друга. `MAX_TABS = 5`: первые 4 пункта —
  прямо в таб-баре, остальное — кнопка "Ещё", открывающая `Modal` со
  списком (`.tabbar-more-list`/`.tabbar-more-item` в `index.css`).
  У бригадира (4 пункта) overflow не срабатывает вообще, поведение не
  меняется
- **Редактирование участков и пользователей (25.09.2026)** — раньше
  название участка (`SitesList.tsx`) и ФИО/роль пользователя
  (`UsersList.tsx`) задавались только при создании, редактировать было
  нельзя. Добавлены кнопки-карандаши (тот же паттерн, что переименование
  статей затрат/организаций бурения 20.09.2026): модалка с полем(ями),
  `.update()`, локальное обновление состояния без перезагрузки списка.
  Права — по явному запросу владельца, ПОДТВЕРЖДЕНО в диалоге: редактировать
  пользователей может management (`isManagement()`, гендир/техдир/
  разработчик) — так же, как везде в проекте, разработчик НЕ исключён из
  этого конкретного права (в отличие от `ROLE_OPTIONS` при создании
  пользователя, где сделать кого-то разработчиком по-прежнему нельзя).
  Новых RLS-политик не потребовалось: `profiles_update_management`
  (миграция 0012) и update-политика на `sites` уже это разрешали
- **Редизайн платформы, фаза 0 — фундамент (25.09.2026, по запросу
  владельца платформы: "переосмысли всё приложение, сделай полный
  редизайн")** — большая многофазная задача, спланирована через Plan Mode
  с явным одобрением владельца, выполняется поэтапно с чек-пойнтами
  (полный план — в истории диалога, не дублируется файлом).
  - **Playwright** добавлен как dev-зависимость: `playwright.config.ts`
    (два профиля — `chromium` десктоп и `mobile` = Pixel 7 на Chromium,
    сознательно НЕ WebKit/iPhone — не тянуть второй движок браузера ради
    одного мобильного профиля), `tests/e2e/public.spec.ts` (без учётных
    данных — экран входа, редирект неавторизованных) и
    `tests/e2e/authenticated.spec.ts` (реальные страницы под логином,
    но АККУРАТНО скипается через `test.skip`, если не заданы
    `TEST_USER_EMAIL`/`TEST_USER_PASSWORD` — эти переменные НИКОГДА не
    хардкодятся в репозиторий, только через `.env.test.local`, который
    уже подпадает под существующий `.gitignore` (`.env.*`/`*.local`).
    Скрипт `npm run test:e2e`
  - **Унификация fade-in анимации**: раньше один и тот же
    initial/animate/transition-триплет framer-motion был скопирован
    руками в ~13 файлов с расходящимися duration/y/шагом задержки, плюс
    существовал параллельный CSS-keyframe `.rise-in` в `index.css`,
    который в итоге НИГДЕ не был подключён (мёртвый код — найден и
    удалён). Теперь один источник — `src/lib/motionVariants.ts`,
    функция `riseIn(index, opts)`, старые вызовы заменены на неё во всех
    13 файлах. `Modal.tsx`/`UserMenu.tsx`/`AppShell.tsx`/`Login.tsx`
    сознательно не тронуты — у них другая, не-списочная анимация
  - Профилактически проверены все `<button>` с `background: 'transparent'`
    и все `<button className="...card...">` по всему проекту на
    задокументированный баг "невидимый текст" (наследование глобального
    `button { color: #fff }`) — новых случаев не найдено, везде уже стоит
    явный `color`
- **Редизайн платформы, фаза 1 — визуальное направление ("Направление
  А", 25.09.2026)** — владельцу показаны через Artifact (Design-канвас)
  два конкретных направления side-by-side на одном и том же фрагменте
  интерфейса: "А — Полевой дневник, версия 2" (эволюция прежнего стиля,
  та же пара шрифтов Fraunces/Manrope/JetBrains Mono) и "Б — Буровая
  консоль" (тёмная индустриальная, Barlow Semi Condensed/IBM Plex Mono).
  **Выбрано А.** Применено в `src/index.css`: фон теплее (пергамент
  `#f3ecdd` вместо кремового `#f5f2ec`), поверхность теплее (`#fffaf0`),
  зелёный акцент углублён (`#2f4a3e` вместо `#3f6259` — лесной, не
  мшистый), терракота вместо глины (`#c1652f` вместо `#a97a52`,
  насыщеннее), радиусы карточек/кнопок увеличены (`--radius-md` 12→14px,
  `--radius-sm` 8→10px), заголовки `h1` крупнее (`clamp(28px,4.5vw,40px)`
  вместо `clamp(26px,4vw,32px)`). Шрифтовая пара НЕ менялась — уже
  признана удачной. Заодно убран последний захардкоженный hex в кнопках
  (`.btn-danger:hover` был `#8a4436` мимо токенов — теперь
  `color-mix(in srgb, var(--color-danger) 80%, black)`, наследует любые
  будущие правки палитры автоматически). Декоративные элементы мокапа
  (топографические изолинии фоном, пунктирная "печать" на карточках) в
  общий прокат СОЗНАТЕЛЬНО не пошли — при повторении на десятках карточек
  по всему приложению превратились бы в шум; если понадобятся — отдельным
  точечным решением, не в базовые токены
- **Редизайн платформы, фаза 2 — навигация (25.09.2026)**:
  - Пользователи/Работники/Организации бурения/Статьи затрат свёрнуты в
    один пункт меню "Настройки" → `src/routes/settings/SettingsHub.tsx`
    (карточки-ссылки на прежние роуты, САМИ роуты не переехали:
    `/users`, `/settings/workers`, `/settings/organizations`,
    `/settings/costs`). Управленческое меню — 9→6 пунктов,
    `BottomTabBar.tsx`'s overflow "Ещё" из-за этого стал прятать 2
    пункта вместо 5. `isNavItemActive()` в `navItems.ts` вручную
    досчитывает `/users` как "внутри /settings" для подсветки активного
    пункта (остальные 3 роута уже покрывались `startsWith`)
  - Перекрёстные ссылки Пользователи ↔ Оргструктура (текстом под
    заголовком каждого экрана) — отвечает на вопрос "зачем два экрана",
    без слияния разных по природе данных (`profiles`/auth vs
    `org_positions`/`workers`, см. запись 24.09.2026)
  - Аудит хлебных крошек на "скрытых" (без пункта меню) экранах нашёл 2
    пропуска: `TaskReportsList.tsx` и `ReportReview.tsx` не имели
    back-ссылки — добавлены по тому же паттерну (`ChevronLeft` +
    `Link`), что уже был везде остальных
- **Редизайн платформы, фаза 3 — прогресс-трекинг (25.09.2026)**:
  - Формулы прогресса по видам работ вынесены из `SiteDetail.tsx` в
    `src/lib/taskProgress.ts` (`drillingProgress`/`coreProgress`/
    `sawingProgress`/`samplingProgress`) — раньше жили только там
    локально, теперь общий источник для `SiteDetail.tsx` И
    `Dashboard.tsx`
  - `Dashboard.tsx`: карточка участка раньше показывала прогресс ТОЛЬКО
    по бурению (кольцо + `ProgressBar`) — это ГЛАВНАЯ метрика,
    остаётся как есть. Добавлена компактная строка бейджей ПОД ней —
    Керн/Распиловка (% от плана) и Опробование (счётчик проб) —
    рендерятся, только если этот вид работ реально есть на участке
  - Новая карточка "Итого по всем участкам" на дашборде (только
    management) — раньше общего числа по компании не было НИГДЕ, даже
    в `SummaryReport.tsx` (тот табличный, без агрегата)
  - `SummaryReport.tsx`: добавлены итоги по распиловке/опробованию —
    пробел, у него в реальности уже приходили строки `reports` для этих
    видов работ (запрос не фильтрует по виду), просто не агрегировались
    в `statTiles`/экспорт. Плюс новый блок "Темп бурения по дням" —
    `src/components/TrendSparkline.tsx`, самописный inline-SVG график
    (та же школа, что `ProgressBar`/`CircularProgress`/
    `WellboreProgress` — библиотеку графиков решили НЕ добавлять,
    расширять свои компоненты)
- **Playwright, e2e-тесты**: см. `tests/e2e/` и `playwright.config.ts`
  (детали — в записи "фаза 0" выше). Держать в актуальном состоянии по
  мере добавления новых экранов — хотя бы `authenticated.spec.ts`'ом
  проверять "открывается без ошибок в консоли", это дёшево и ловит
  минимум регрессии вроде сломанного импорта

- **Оргструктура: pan/zoom вместо горизонтальной прокрутки (25.09.2026,
  по прямому отзыву владельца платформы — "линии заходят на карточки,
  вся схема не видна")** — предыдущая мобильная адаптация (сжатие узлов
  под конкретный брейкпоинт, запись выше) не решила проблему по существу:
  с реальным составом штата дерево всё равно не помещалось целиком, а
  overflow-x:auto был неудобен и на десктопе. Заменено на
  `src/components/PanZoomViewport.tsx` — самописный (без сторонней
  библиотеки, тот же принцип, что и `TrendSparkline`) pan/zoom-контейнер:
  - Автоподгонка масштаба под весь холст при загрузке (`fitToView`,
    измеряет `canvas.scrollWidth/Height` БЕЗ текущего transform, чтобы не
    искажать замер предыдущим масштабом) — вся схема видна целиком сразу
    при открытии, без единого пикселя прокрутки
  - `ResizeObserver` на ДВА элемента — canvas (контент — добавили узел)
    И сам viewport (изменился размер контейнера — поворот экрана, ресайз
    окна). Изначально наблюдали только canvas — при переключении на
    мобильную ширину без перезагрузки страницы масштаб оставался
    посчитанным под старую (десктопную) ширину, почти всё дерево
    пропадало за кадром — поймано и исправлено в этом же заходе
  - Zoom колесом мыши (к курсору) и кнопками +/−/"показать всю схему" в
    углу (к центру viewport), drag для панорамирования (Pointer Events —
    единый код для мыши и тача)
  - Клик по узлу дерева отличается от завершения drag через порог
    смещения (`moved`, 4px) + гашение одного клика на capture-фазе сразу
    после реального перетаскивания (`suppressNextClick`) — иначе
    отпускание мыши в конце drag открывало бы модалку узла под курсором
  - Узлы дерева (`.org-node-btn`, коннекторы) вернули к десктопному
    размеру ВЕЗДЕ — отдельный мобильный `@media`, уменьшавший шрифт/
    отступы под конкретную ширину экрана, убран целиком: под любой экран
    подстраивается общий масштаб через `transform: scale()`, а не два
    параллельных набора CSS-размеров
  - Высота `.pan-zoom-viewport` — в CSS (`520px` десктоп / `420px` на
    `max-width:767px`), НЕ пропом/инлайн-стилем на компоненте: инлайн-
    стиль specificity всегда выше обычного правила, media query не смог
    бы её переопределить, если бы высота приходила через `style={{height}}`

- **Оргструктура и Пользователи переведены на person-centric модель
  (25.09.2026, по запросу владельца — прежняя связка "дерево вакантных
  слотов `org_positions`" + отдельно "Пользователи"/"Работники" казалась
  запутанной, владелец явно попросил, чтобы Оргструктура "базировалась на"
  Пользователях)** — миграция `0016_org_chart_person_centric.sql`.
  Зафиксированы 4 развилки (диалог 25.09.2026): (1) должность — отдельный
  справочник, НЕ то же самое, что `profiles.role` (роль = права доступа,
  не трогаем); (2) Пользователи/Работники остаются раздельными вкладками
  (не объединяем в общий список персонала); (3) **вакантные должности
  упразднены** — дерево строится от людей, у каждого свои поля
  "должность"/"руководитель", узел без назначенного человека в схеме
  просто не существует; (4) правка иерархии остаётся формой (select
  "Руководитель"), drag-and-drop сознательно отложен.
  - Новая таблица `positions` (id, name) — общий справочник названий
    должностей для `profiles` И `workers` одновременно, тот же RLS-паттерн,
    что у `cost_categories`/`drilling_organizations` (select всем, write —
    `is_management()`). Управляется из "Пользователи" → кнопка "Управление
    должностями" (`PositionsManagerModal` в `UsersList.tsx`) — плоский
    add/rename список, не двухуровневый, как у статей затрат.
  - И `profiles`, и `workers` получили одинаковый набор новых колонок:
    `position_id` (FK на `positions`, `on delete set null`) +
    `reports_to_profile_id`/`reports_to_worker_id` (ровно один из двух или
    ни одного — `check num_nonnulls(...) <= 1`, тот же приём, что был у
    `org_positions.assigned_worker_id`/`assigned_profile_id`) + guard от
    самоссылки (`*_no_self_report`). `on delete set null` — сознательное
    отступление от обычной для проекта философии "блокировать удаление,
    защищать историю" (`author_id`/`foreman_id`/`created_by`): это не
    операционная история, а живой снимок "кто сейчас кому подчиняется" —
    при удалении руководителя цепочка просто обрывается.
  - **`workers.assigned_foreman_id` НЕ тронут и НЕ переиспользован** как
    "руководитель" — это отдельное операционное поле (текущая буровая
    бригада для `task_worker_assignments`/`CrewAssignmentSection.tsx`, со
    своей RLS с 19-20.09.2026). Геолог, например, может не входить ни в
    одну буровую бригаду (`assigned_foreman_id = null`), но иметь
    начальника в оргструктуре компании — два разных отношения по смыслу.
  - `workers.position` (свободный текст, с 19.09.2026) перенесена в
    `position_id` через join по точному совпадению названия, затем колонка
    дропнута тем же файлом миграции.
  - **Перенос существующих данных из `org_positions`** — не наивным
    копированием `parent_id` (у большинства сидовых узлов родитель
    вакантен), а рекурсивным CTE, поднимающимся до БЛИЖАЙШЕГО ПРЕДКА С
    НАЗНАЧЕННЫМ ЧЕЛОВЕКОМ (пропуская вакантные промежуточные узлы) —
    иначе почти все реальные люди остались бы без руководителя после
    переноса. Названия всех узлов (включая вакантные) сидируются в
    `positions`, чтобы словарь не обеднел вакансиями, которые исчезнут.
  - **`org_positions` НЕ дропается в миграции 0016** — таблица больше не
    читается кодом, но сама остаётся до отдельного, следующего файла
    `0017_drop_org_positions.sql`, который нужно применять только ПОСЛЕ
    того, как вживую проверено, что перенос прошёл верно (необратимая
    операция сразу за миграцией со сложным CTE — разумно сначала
    посмотреть на данные).
  - **Прямое и ожидаемое следствие пункта (3) выше**: после применения
    0016 большинство вакантных узлов сидовой схемы (~11 из 16 — "Ведущий
    инженер-геотехник", "Инженер-геотехник", "Ведущий-геолог", "Геолог",
    два "Техник-геолог", "Водитель Урал-5557", "Помощник машиниста" и
    т.п.) пропадают из "Оргструктуры" сразу — не баг, это то, что владелец
    выбрал явно, вернуть можно назначением реального человека на ту же
    должность через "Пользователи"/"Работники".
  - Новые общие модули (используются сразу в 3 местах — `UsersList.tsx`,
    `WorkersSettings.tsx`, `OrgChart.tsx`): `src/lib/personRef.ts`
    (префиксные значения `profile:<uuid>`/`worker:<uuid>` для
    объединённых select'ов, `buildPersonNodes`/`collectDescendantKeys` —
    обобщение прежнего локального `collectDescendantIds` из `OrgChart.tsx`
    на объединённый список людей вместо одной таблицы) и
    `src/components/PersonSelect.tsx` (select с `<optgroup>`
    "Пользователи"/"Работники", `excludeKeys` — защита от цикла в
    иерархии: нельзя назначить руководителем самого себя или своего же
    подчинённого).
  - `UsersList.tsx`/`WorkersSettings.tsx` — формы создания/правки
    получили поля "Должность" (select из `positions`) и "Руководитель"
    (`PersonSelect`). У `WorkersSettings.tsx` карточка работника впервые
    получила полноценную форму правки (`Modal` + карандаш) — раньше там
    было только создание + инлайн-смена бригадира.
  - **`OrgChart.tsx` переписан полностью** — больше не читает
    `org_positions` вообще. Дерево строится из `profiles`+`workers`,
    видимость узла ⟺ `position_id` у человека задан; человек-руководитель
    без своей должности не показан в схеме — его подчинённые в этом
    случае считаются корнями дерева (не теряются). `submits_reports`
    (оранжевая рамка) стал ВЫЧИСЛЯЕМЫМ (`role === 'party_chief'`), а не
    хранимым полем — сводки реально подают только начальники буровой
    партии. Клик по узлу открывает ту же форму "должность + руководитель",
    что и в Пользователях/Работниках — пишет напрямую в `profiles`/
    `workers`, это и есть обещанная "синхронизация в обе стороны" (одни и
    те же колонки БД, два экрана для их правки). Добавление НОВОГО
    человека больше не делается на этом экране — кнопки в шапке ведут в
    "Пользователи"/"Работники". "Убрать из схемы" = очистить `position_id`
    (не удаляет самого человека) — без подтверждения, действие обратимое
    (можно назначить обратно). `PanZoomViewport`/CSS-дерево (`.org-tree`)
    не тронуты вообще — поменялись только данные и модалка.

- **Роль "Ответственный" (переименование, 25.09.2026, по запросу владельца)**
  — подпись роли `party_chief` в `ROLE_LABELS` изменена с "Начальник
  буровой партии (бригадир)" на "Ответственный": владелец обнаружил, что
  название роли путает с должностью, хотя по факту право роли — "может
  быть назначен ответственным за задание и отправлять по нему сводки", не
  привязано к конкретной должности "бригадир" (эту роль можно выдать и
  мастеру, и машинисту). Сам identifier `party_chief` в БД/коде НЕ
  переименован — RLS нигде не сравнивает роль с литералом напрямую (только
  через `foreman_id`/`assigned_party_chief_id` конкретного задания), а
  переименование enum потребовало бы синхронной правки ~13 файлов ради
  нулевой функциональной выгоды. Синхронизированы формулировки в
  `CoreDescriptionTaskForm.tsx`, `DailyReportForm.tsx`, `MyReports.tsx`,
  легенда в `OrgChart.tsx`. **НЕ тронуты**: лейбл "Бригадир" на
  `DrillingTaskForm.tsx` (контекстное поле "кто ведёт эту буровую», не
  название роли) и "бригадир"/`assigned_foreman_id` в `WorkersSettings.tsx`
  (отдельное операционное поле с 19.09.2026, не эта роль).
- **"Выдать доступ" — превратить работника в пользователя без повторного
  ввода ФИО (25.09.2026)** — миграция `0018_profiles_person_registry_link.sql`.
  Владелец описал реальную проблему: "Пользователи" и "Работники" по сути
  один и тот же список людей, искусственно разделённый (Supabase Auth
  требует отдельную строку `auth.users` на каждого логинящегося человека,
  `profiles.id` обязан ей совпадать — слить в одну таблицу с общим id
  нельзя физически, это НЕ прихоть дизайна). Решение — не сливать таблицы,
  а связать: `profiles.person_id → workers(id)` (nullable, `unique` — один
  работник может получить логин только один раз). Историю НЕ переносим
  (сознательно, ниже риск, чем переписывать её задним числом): старая
  строка `workers` остаётся под своим id, `task_worker_assignments.
  worker_id`/чужие `reports_to_worker_id` продолжают резолвить её имя как
  раньше — просто архивируется (`archived_at`) и теряет `position_id` (те
  же механизмы, что обычное архивирование и "Убрать из схемы" в
  `OrgChart.tsx`), чтобы не остаться дублирующим узлом в схеме.
  - Общий хелпер `src/lib/grantAccess.ts` (`grantAccessToWorker`) —
    используется и кнопкой-ключом на карточке в `WorkersSettings.tsx`, и
    режимом "Существующий работник" в форме создания в `UsersList.tsx`
    (переключатель "Новый человек"/"Существующий работник" — во втором
    режиме ФИО не печатается заново, а выбирается из реестра работников,
    должность/руководитель подхватываются, но остаются редактируемыми).
  - Попутно найден и починен реальный, ранее незамеченный баг: колонка
    `workers.archived_at` (должна была появиться ещё в миграции 0014,
    23.09.2026) физически отсутствовала в живой базе — архивирование
    работников тихо не сохранялось (UI обновлялся оптимистично через
    локальный `setState`, но `handleArchiveToggle` не проверял ошибку
    `.update()`, поэтому ничего не заметили). Обнаружено ровно в момент
    тестирования "Выдать доступ" (его второй шаг — архивирование —
    начал возвращать `column does not exist`). Почему остальные части
    миграции 0014 (например, `profiles_delete_management`) при этом
    работали — не выяснено; чинится идемпотентно:
    `alter table workers add column if not exists archived_at timestamptz;`.
    **Если где-то ещё найдётся расхождение между тем, что миграция должна
    была сделать, и живой базой — не удивляться, применять точечный
    idempotent-фикс, а не переигрывать всю миграцию заново.**
  - Проверено вживую целиком (создание тестового работника с должностью →
    "Выдать доступ" → новый профиль с унаследованной должностью и
    `person_id` → исходная запись работника архивирована и без должности →
    ровно один узел в схеме, без дублей → тестовые данные удалены).
- **"Без назначенной должности" + "Добавить человека" прямо в Оргструктуре
  (25.09.2026, по запросу владельца)** — жалоба "из четырёх пользователей
  показывается только три" оказалась НЕ багом кода: у Бегзатова Нурлана
  Асхатовича `position_id` был `null` с самого начала (в старой
  `org_positions`-модели он никогда не был назначен ни на один слот) — по
  дизайну (см. запись про 0016) человек без должности в схеме не
  отображается. Проблема была в обнаруживаемости — ничего на экране не
  подсказывало, что такой человек вообще существует и просто не привязан.
  - Новый раздел под деревом — "Без назначенной должности": все
    profiles/workers (без архивных, без разработчика) с `position_id ==
    null`, каждый рендерится как `ChartPerson` с пустым `positionId` —
    клик открывает ТУ ЖЕ форму "должность + руководитель"
    (`openNode`/`handleSave` не менялись вообще, просто впервые получают
    person с `positionId: ''`). Заголовок модалки меняется на "Назначить
    должность: {имя}", кнопка "Убрать из схемы" скрыта (нечего убирать).
  - Новая кнопка "+ Добавить человека" в шапке — модалка с переключателем
    "Работник"/"Пользователь" прямо в Оргструктуре, без ухода на другие
    экраны. Сознательно НЕ спрашивает должность/руководителя при
    создании — новый человек появляется в разделе "Без назначенной
    должности" сразу после, назначение остаётся отдельным явным шагом
    (ровно так описал задачу владелец: "добавление карточек... с
    последующим прикреплением по ветке"). "Пользователь" переиспользует
    новый `createUserFromScratch` (вынесен в `grantAccess.ts` из инлайна
    в `UsersList.tsx` — та же логика `signUp`+`insert`, вынесена в общую
    `signUpAndInsertProfile`); "Работник" — прямой `insert` в `workers`.
    **`UsersList.tsx`'s "Новый человек" НЕ переведён на этот хелпер** —
    та форма сознательно позволяет задать должность/руководителя сразу
    при создании (другой UX, менять не просили) — рефактор туда внёс бы
    риск регресса без пользы.
  - Ссылки в шапке экрана переименованы: "Добавить пользователя"/
    "Добавить работника" (вели на другие экраны) → "Все пользователи"/
    "Все работники" (тот же переход, но теперь это явно не единственный
    способ добавить человека).
  - Реальная должность/руководитель Бегзатова оставлены НЕ назначенными
    сознательно — это кадровое решение владельца, не то, что должен
    решать код молча.

- **Редактирование организаций бурения и станков (25.09.2026, по запросу
  владельца)** — `DrillingOrganizationsSettings.tsx` умел только
  ДОБАВЛЯТЬ организации/станки, не редактировать уже созданные (владелец
  спросил, где править список станков, и обнаружил, что такой
  возможности нет вообще). Добавлена кнопка-карандаш у каждой организации
  (в шапке карточки) и у каждого станка внутри неё (в строке списка) —
  обе открывают ту же `Modal`, что и добавление, только предзаполненную
  текущими значениями и делающую `.update()` вместо `.insert()`. Один в
  один повторяет паттерн, уже обкатанный в `CostCategoriesSettings.tsx`
  20.09.2026 (переименование категорий/видов затрат): общая мини-форма
  полей вынесена в `RigFields` (аналог `ItemFields`), `drillingTypeToSelectValue()`
  (аналог `unitToSelectValue()`) корректно подставляет "другой…" в селект
  вида бурения, если у существующего станка тип заведён вручную и не входит
  в стандартный список — иначе редактирование молча откатило бы его на
  первый вариант. Шапка карточки организации по той же причине, что и в
  `CostCategoriesSettings.tsx` (нельзя вложить `<button>` в `<button>`),
  переструктурирована на `<div>` с раскрывающей и переименовывающей
  кнопками рядом, а не одним составным `<button>` на всю ширину.
  Редактируется только `name` организации — `is_own` (какая организация
  "своя") через интерфейс не меняется, это отдельное, более чувствительное
  решение, которое не просили трогать.

- **Состав бригады: смены + история замен (25.09.2026, по запросу мастера
  участка)** — миграция `0019_task_worker_assignments_shifts_and_history.sql`.
  Раньше `task_worker_assignments` (миграция 0010) знала только "кто СЕЙЧАС
  в этой роли" — без деления по сменам и без истории: замена работника была
  обычным удалением старой строки и вставкой новой, прежний состав терялся
  безвозвратно. Два реальных сценария не были покрыты: (1) на станке две
  смены с разными буровиком/помбуром каждая; (2) периодическая замена
  работника (временная подмена или насовсем).
  - `shift_number` (1|2, nullable) — заполняется ТОЛЬКО для `driller`/
    `assistant_driller` (буровик/помбур реально работают посменно);
    `responsible` (керн/распиловка/опробование) остаётся без смены,
    поведение для него не изменилось вообще — сознательно не расширяли
    смены на роли, где заказчик их не просил (хотя у распиловки в сводках
    уже есть "День"/"Ночь" — если потребуется, добавить туда смены будет
    тем же полем, без новой миграции).
  - `valid_from`/`valid_to` — замена = закрыть старую строку (`valid_to` =
    сегодня) + открыть новую (`valid_from` = сегодня, `valid_to = null`),
    а не перезаписать. Работает одинаково для временной и постоянной
    замены — разница только в том, откроется ли позже новая строка на
    прежнего работника. `valid_to = null` теперь и есть определение
    "работает сейчас". Старые 4 частичных уникальных индекса (по одному на
    вид задания, на `(task_id, role, worker_id)`) заменены на такие же 4,
    но по `(task_id, role, coalesce(shift_number, 0), worker_id) where
    valid_to is null` — тот же работник может законно повторно появиться в
    истории, а смена входит в ключ, чтобы буровик мог одновременно
    числиться в разных сменах/заданиях.
  - `src/components/CrewAssignmentSection.tsx` — переписан: буровик/помбур
    теперь рендерятся как ДВЕ независимые под-секции (Смена 1/Смена 2) с
    отдельными списками и отдельным "+ добавить работника" на каждую;
    `responsible` рендерится как раньше, одним списком. "Убрать" (крестик
    на чипе) больше не удаляет строку — делает `.update({valid_to:
    сегодня})`, сохраняя историю. Внизу секции — сворачиваемая "История
    замен (N)" со списком закрытых назначений (роль, смена, работник,
    период `valid_from – valid_to`). RLS не менялась —
    `task_worker_assignments_own_foreman`/`_write_management` уже были
    `for all`, автоматически покрывают UPDATE теми же условиями, что раньше
    покрывали INSERT/DELETE.
  - Проверено вживую самим мастером участка сразу после применения
    миграции (не тестовыми данными — реальное назначение бригады на
    скважину ZKY-11501): буровик смены 1 и смены 2 — разные люди, замена
    буровика смены 2 (закрытие одного, назначение другого) корректно ушла
    в историю с правильным периодом действия.

- **Метраж бурения снова "от"/"до" вместо одного числа (25.09.2026, по
  отзыву мастера участка)** — `DailyReportForm.tsx`. Мастер, впервые
  заполняя реальную сводку, ожидал увидеть интервальный ввод забоя, как у
  описания керна ("от" — автоподставляется и не редактируется, "до" —
  вручную), а видел только одно поле "Метраж бурения за смену, м" с
  текстовой подсказкой по забою рядом (решение 20.09.2026). Схема БД НЕ
  менялась — `reports.drilling_meters` остаётся дельтой за смену; форма
  теперь хранит `drillingFrom`/`drillingTo` (забой на начало/конец смены) и
  считает `drilling_meters = drillingTo - drillingFrom` перед отправкой —
  то же самое число, что раньше вводили напрямую, просто по-другому
  считается на экране.
  - `drillingFrom` — read-only, тем же принципом, что `coreFrom`/`photoFrom`
    у описания керна: сумма `drilling_meters` по ВСЕМ предыдущим сводкам
    задания (submitted+approved), т.е. текущий `knownMeters`. При правке
    уже существующей сводки эта сумма явно считается БЕЗ самой правимой
    строки (`otherReports = taskReports.filter(r => r.id !== reportId)`)
    — иначе "от" при открытии на правку показал бы забой уже НА КОНЕЦ этой
    смены, а не на начало. `drillingTo` при правке восстанавливается как
    `knownSumAtLoad + r.drilling_meters` (та же сумма без этой строки, плюс
    её собственный метраж) — так после открытия старой сводки на
    редактирование интервал показывает ровно то, что было при первой
    подаче, а не текущий (изменившийся) накопленный забой.
  - После успешной отправки новой (не правки) сводки `drillingFrom`
    автоматически сдвигается на только что введённый `drillingTo` — при
    заполнении смены 2 сразу следом "от" уже верный, без перезагрузки
    страницы (тот же приём, что у coreFrom/photoFrom).
  - Подсказка "не согласован" (красная точка, отличие `knownMeters` от
    `priorApprovedMeters`) и хинт по суточному плану — оставлены, просто
    переехали под поля от/до внутрь `<fieldset>` вместо инлайн-текста в
    `<label>`. Расчёт `bottomHole` для WhatsApp-сообщения НЕ менялся
    (по-прежнему `priorApprovedMeters + delta`, только согласованные
    смены) — сознательно, тот же принцип, что и в решении 20.09.2026.
  - Проверено вживую самим мастером на реальной сводке (скважина
    ZKY-11501): "от" корректно показал накопленный забой 516.9 м из уже
    поданной сводки. По ходу проверки поймали и перезапуском dev-сервера
    почини́ли уже задокументированный класс проблемы этой среды —
    OneDrive-синхронизация вызывает битые HMR-трансформы Vite
    (`ReferenceError` на переменные, которых в актуальном файле уже нет) —
    не баг в текущем коде, `npm run build`/`tsc -b` их не ловят, т.к. их и
    нет в реальном файле.

- **Округление метров до сотых — "7.149999999999977 м" (25.09.2026, по
  жалобе владельца на реальные данные мастера)** — прямое следствие
  вычитания чисел с плавающей точкой в JS (`Number(до) - Number(от)` в
  новом интервальном вводе метража, см. запись выше) — обычная проблема
  IEEE 754, не баг конкретно этой формы. `round2()` (новый экспорт в
  `src/lib/taskProgress.ts`, `Math.round((n + Number.EPSILON) * 100) / 100`)
  теперь применяется в двух слоях:
  - **У источника** — `DailyReportForm.tsx` округляет саму дельту
    (`drilling_meters` при сохранении), сумму "от" при загрузке
    (`approvedSum`/`knownSum`), приращение `knownMeters` после отправки и
    `bottomHole` для WhatsApp-сообщения — новые сводки больше не сохраняют
    длинные хвосты.
  - **При отображении** (защита от уже накопленного мусора и от
    сложения/вычитания уже округлённых чисел, которое само по себе тоже
    может дать копеечную погрешность) — `taskProgress.ts` (все 4 функции
    прогресса), `TaskDashboard.tsx` (ячейки "По дням", "Итого/день",
    "Накопл. факт", "план"/"отклонение" — заодно точность там поднята с
    `.toFixed(1)` до `.toFixed(2)`, раз уж просили именно сотые),
    `SummaryReport.tsx` (все statTiles и спарклайн темпа), `ReportDetail.tsx`/
    `ReportReview.tsx` (метраж/интервалы/забой одной сводки), `ProgressBar.tsx`
    и `WellboreProgress.tsx` (общие компоненты — округление здесь разом
    закрывает всех вызывающих, включая `Dashboard.tsx`/`SiteDetail.tsx`).
  - **Уже испорченное значение в реальных данных** (сводка мастера от
    16.09.2026, смена 2, `675261ae-...`, буквально `7.149999999999977`)
    поправить кодом было НЕЛЬЗЯ — сессия, которой я мог воспользоваться,
    залогинена как автор этой сводки, а RLS (`reports_update_own_when_
    editable`) не даёт трогать уже `submitted`-строку даже автору. Дан
    владельцу разовый (не миграция схемы) SQL для ручного запуска через
    SQL Editor — округляет задним числом ВСЕ float-колонки `reports`
    (`drilling_meters`, `sawn_meters`, обе пары интервалов, `hours_worked`),
    идемпотентно (на уже чистых числах ничего не меняет). Проверено вживую
    (та же сессия автора): экран "По дням" уже показывает "7.15 м" вместо
    мусора ДО применения этого SQL — округление при отображении работает
    независимо от того, почищена ли сама база; сам SQL нужен, чтобы не
    носить мусор в исходном значении дальше (экспорт xlsx и т.п. читают
    сырую колонку напрямую, без прохода через эти компоненты).

## Не реализовано (сознательно отложено, не путать с "забыли")

- **Офлайн-режим** (IndexedDB, очередь синхронизации) — самая трудоёмкая
  часть, отдельный будущий этап
- **Табель** — структура не определена, заказчик отложил вопрос
- Управление справочниками (организации-подрядчики, категории затрат) —
  сейчас правятся только через SQL/Table Editor в Supabase напрямую, экрана
  в UI нет
- **Автогенерация сводного отчёта заказчику** (по всем скважинам/заданиям
  участка сразу, формат из переписки 17.09.2026) — сознательно вторым
  этапом, после того как схема заданий (распиловка/опробование/документация)
  обкатается на реальных данных

## Workflow

- Ветка на фичу → PR → **обязательно `npm run build` и `npx tsc --noEmit`
  перед коммитом** → описание PR по-русски, подробное, с объяснением "почему",
  не только "что"
- Если PR меняет `supabase/migrations/` — миграцию нужно ОТДЕЛЬНО применить
  в Supabase SQL Editor (владелец проекта делает это вручную, скопировав SQL)
  — само по себе она не применяется автоматически нигде
- Полное ТЗ (все версии решений, включая отклонённые варианты и почему) —
  в истории переписки с заказчиком в claude.ai, не продублировано в репозитории
  файлом. Если нужны детали, которых нет в этом файле — переспросить заказчика,
  не додумывать самостоятельно

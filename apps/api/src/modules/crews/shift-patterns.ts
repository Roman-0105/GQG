/**
 * Ограниченный набор графиков вместо свободного текста — было поле
 * "напиши как хочешь", попросили выпадающий список (замечание владельца
 * компании). Значения — рабочие коды, подписи — на фронтенде
 * (apps/web/src/lib/labels.ts), чтобы не дублировать русский текст в
 * двух местах.
 */
export const SHIFT_PATTERNS = ['15/15', '30/30', 'floating'] as const;
export type ShiftPattern = (typeof SHIFT_PATTERNS)[number];

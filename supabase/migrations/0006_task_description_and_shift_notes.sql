-- ============================================================================
-- Этап 6: калибровка механики заданий и сводок (без изменения RLS —
-- новые поля читаются/пишутся теми же политиками, что и вся остальная
-- строка соответствующей таблицы).
--
-- 1. Описание задания — свободный текст для техдира/гендира при постановке
--    задачи бригадиру (что именно делать, на что обратить внимание).
-- 2. План бурения по задаче — задаётся ОДНИМ числом (метров в сутки), а не
--    календарём на каждый день. Сознательный выбор: реальный план буровой
--    задаётся именно как темп ("столько-то метров в сутки"), а не разными
--    цифрами на каждую дату по отдельности. Накопительный план на дату X
--    считается на фронте как min(planned_daily_meters * дней с начала,
--    projected_depth) — см. src/routes/tasks/TaskDashboard.tsx. Если
--    практика покажет, что темп плавающий (сезонность, разные этапы) —
--    тогда заводить отдельную таблицу план/дата, но не раньше.
-- 3. shift_notes на сводке — короткое описание смены от бригадира: что
--    сделано, что помешало плану (замена коронки, авария и т.п.), см. ТЗ
--    (сообщение заказчика от 17.09.2026).
-- ============================================================================

alter table drilling_tasks add column description text;
alter table drilling_tasks add column planned_daily_meters numeric;

comment on column drilling_tasks.planned_daily_meters is
  'План бурения, м/сутки. Накопительный план на дату — считается на фронте, не хранится.';

alter table core_description_tasks add column description text;

alter table reports add column shift_notes text;

comment on column reports.shift_notes is
  'Короткое описание смены от бригадира: что сделано, что помешало плану.';

-- ============================================================================
-- Этап 2: стартовые данные справочников.
-- Не обязательно, но ускоряет первые тесты — можно менять/дополнять
-- как угодно прямо в Table Editor после применения, без новой миграции.
-- ============================================================================

insert into drilling_organizations (name, is_own) values
  ('Собственная буровая бригада', true)
on conflict (name) do nothing;

insert into cost_categories (name, unit) values
  ('ГСМ', 'л'),
  ('Коронки', 'шт'),
  ('Материалы (прочее)', 'шт')
on conflict (name) do nothing;

-- ============================================================================
-- Этап 17 (25.09.2026) — завершение перехода на person-centric оргструктуру
-- (см. 0016). Применять ОТДЕЛЬНО и ТОЛЬКО после того, как вы проверили в
-- приложении, что должности/руководители у людей выглядят правильно
-- (Пользователи/Работники/Оргструктура). Это необратимая операция — данные
-- из org_positions уже перенесены миграцией 0016, здесь просто убирается
-- отслужившая таблица.
-- ============================================================================

drop table org_positions;
